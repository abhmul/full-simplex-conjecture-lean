# Current implementation state

Updated 2026-09-08. The user appointed root as implementation and Git owner and set an unbudgeted FSC certification goal. Work is active; no final FSC comparison or equality theorem is proved.

WP00 real source/API consumers and actual Lean JSON audit calibration passed. WP01 shared public/pin/support/q/Peano definitions and elementary measure bridges passed warning-clean builds and fresh axiom auditing. WP02 all-PSD affine/independent-sum laws and ordinary Gram realization, WP03 actual one-pin disintegration, WP08 finite-second-moment noise averaging, WP14 componentwise scalar comparison/equality and WP20 compact barrier are independently source-reviewed and freshly audited with the permitted axioms. Product partial-to-C2 and vector-domain C2-to-Peano adapters are proved; they leave their actual regularity hypotheses explicit. The historical BOOTSTRAP_VERIFICATION.json remains bootstrap evidence only.

The singular triangle's concrete certificate is closed: actual Gaussian score/pair laws, canonical weights, redundant support calculus, full vector-domain Peano expansion, actual normalized-addition right derivative −(2/5)q₁₂, strict sign, correlation feasibility and rank increase from 2 to 3. WP04 row-maximal positivity on general correlations, WP11 unbounded-cap dilation/strict energy/exact Euclidean padding and WP26 finite-input calculus also passed independent review and fresh audits. The square strip gives exactly 2H−2tφ(t), including the unused-coordinate H, with strictly positive normalized energy. These results do not yet establish the universal FSC derivative or public comparison/equality.

Actual unit-normal support C1 on a common neighborhood and the quadratic support tangent are reviewed and freshly audited. The latter uses public pinned Prekopa and imposes no differentiability or tie exclusion at the reference. All-correlation joint CDF continuity and compactness now yield an actual minimizer at every real threshold. Singular unit Gram realization in dimension n−1 and tangent coordinates in dimension n−2 are reviewed; actual projected-residual transport into that subspace is the next facet consumer. WP09 supplies the exact normalized-addition noise-average law; its derivative assembly retains explicit Peano/Hessian inputs and stays outside the public root until the universal producer discharges them.

Active ownership: root owns integration and WP17 simplex recursion; wp00_algebra owns WP21 simplex foundations/base cases; wp00_imports owns WP16 facet compatibility and subspace Gaussian bridge; analytic_preflight owns WP07 projected normals, actual conditional caps and universal threshold expansion. Worker cards record exact disjoint files. Compiler evidence justified the generic calculus and actual-law module splits; the graph is refinable and no package-count stopping quota applies.

Mathematics: the support/noise variant is independently analytically accepted with the domain clarifications in MATHEMATICAL_REVIEW.md. Both explanatory teaching proofs remain supplied. The original regularized covariance route is fallback, not a parallel required implementation. No architectural consultation is presently required before trying the actual compiler interfaces.

Ownership: the lead owns this repository's integration and local commits; workers need explicit disjoint file assignments. The research coordinator remains outside Lean implementation and reviews mathematical contract/authority changes. The old WSC checkout and research repository are read-only to the new owner. No remote, publication, new-work license or resource expansion is authorized.

Tools: Lean 4.31.0 is installed through /usr/bin/lean and /usr/bin/lake. No native LSP tool was exposed. With explicit user authorization, root installed leanclient 0.13.2 locally under .lake and verified actual Lean server goals/diagnostics through scripts/lean_lsp.py; see LEAN_TOOLING.md. Python uses the documented specialized interpreter. Mathematical dependencies and global agent configuration are unchanged.

Stop on a genuine mathematical or authority dependency, with exact goals and minimal reproductions, while continuing independent in-scope packages when possible. Do not call this bootstrap or a conditional assembly a formal proof.
