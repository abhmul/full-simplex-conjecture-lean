# Current implementation state

Updated 2026-09-08. The user appointed root as implementation and Git owner and set an unbudgeted FSC certification goal. Work is active; no final FSC comparison or equality theorem is proved.

WP00 real source/API consumers and actual Lean JSON audit calibration passed. WP01 shared public/pin/support/q/Peano definitions and elementary measure bridges passed warning-clean builds and fresh axiom auditing. WP14 full componentwise scalar comparison/equality passed independent source review and fresh expanded-statement/axiom checks; its zero-component adverse case is formally verified. The triangle has compiled actual Gaussian score and deterministic pair-law/probability subcases plus redundant unbounded cap mass and support derivatives; its second-order expansion and rank-increasing derivative remain unpaid. The historical BOOTSTRAP_VERIFICATION.json is retained unchanged as bootstrap evidence.

Active ownership: root integrates shared definitions, evidence and the compact barrier; worker wp00_algebra owns WP02 affine/ordinary Gram laws after completing WP14; worker wp00_imports owns WP08 generic noise averaging; worker analytic_preflight owns WP10 singular triangle. Worker cards record exact file ownership. Next critical consumers are threshold support regularity and Peano expansion, then the actual normalized PSD-addition derivative. Follow WORK_PACKAGES.md; the graph is refinable and no package-count stopping quota applies.

Mathematics: the support/noise variant is independently analytically accepted with the domain clarifications in MATHEMATICAL_REVIEW.md. Both explanatory teaching proofs remain supplied. The original regularized covariance route is fallback, not a parallel required implementation. No architectural consultation is presently required before trying the actual compiler interfaces.

Ownership: the lead owns this repository's integration and local commits; workers need explicit disjoint file assignments. The research coordinator remains outside Lean implementation and reviews mathematical contract/authority changes. The old WSC checkout and research repository are read-only to the new owner. No remote, publication, new-work license or resource expansion is authorized.

Tools: Lean 4.31.0 is installed through /usr/bin/lean and /usr/bin/lake. No native LSP tool was exposed. With explicit user authorization, root installed leanclient 0.13.2 locally under .lake and verified actual Lean server goals/diagnostics through scripts/lean_lsp.py; see LEAN_TOOLING.md. Python uses the documented specialized interpreter. Mathematical dependencies and global agent configuration are unchanged.

Stop on a genuine mathematical or authority dependency, with exact goals and minimal reproductions, while continuing independent in-scope packages when possible. Do not call this bootstrap or a conditional assembly a formal proof.
