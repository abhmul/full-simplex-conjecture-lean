# FSC implementation acceptance

The exact public theorem source is implemented and independently reviewed. The complete current-checkout gates passed; artifact-free source reconstruction remains pending. Publication, external acceptance and new-work licensing are separate operator decisions.

The public import is `FSC`. Accepted statements are `cdf_simplex_le` for every n≥2, every PSD correlation and every real threshold; `cdf_eq_simplex_iff` at any fixed positive threshold; `cdf_simplex_lt` for nonsimplex correlations at positive threshold; actual Gaussian lower-orthant comparison; and coordinate-maximum upper-tail comparison, strictness and equality. The maximum upper-tail direction is arbitrary correlation ≤ simplex. No rank, distinctness, balance, minimum or analytic-certificate hypothesis remains.

Independent expanded consumers use literal Euclidean Gaussian measures, separate PSD and diagonal-one hypotheses, the displayed `(1/(n-1)) • (n • I - J)` with real subtraction, and literal existential coordinate exceedance for the maximum tail. See [statement review](work-packages/WP25-statement-review.md). [Provenance review](work-packages/WP25-provenance-review.md) verifies all ten dependency pins, original source hashes and retained licenses for the two narrow adapted templates. [Runner review](work-packages/WP25-runner-review.md) records acceptance controls and resolved findings.

The current-checkout runner is `scripts/verify_release.py`, invoked with the documented specialized Python and a fresh evidence path. Its successful receipt is checks/release/current-gates-v2/verification.json. It runs a warning-clean full `FSC FSCProbes` build, a separate root compilation, the fresh coverage-checked 45-declaration public audit, 17 independent expanded declaration audits, 27 regression audit files and positive/negative audit calibration. Only subsets of propext/Classical.choice/Quot.sound are allowed. The source scan complements this transitive audit. No copied or historical Lean report is accepted by the runner.

The first complete-suite attempt rejected early unqualified audit command names against Lean's fully qualified actual reports. The three affected adverse files now use fully qualified names; proof bodies and the parser are unchanged. Failed evidence remains in checks/release/current-gates. The runner also rejects Python optimization, stale evidence directories, missing required regression endpoints, dirty clean-reconstruction source, changed source identity and startup compiled artifacts.

The clean reconstruction procedure creates independent Git object/source clones using `scripts/prepare_clean_checkout.py --destination <fresh>/checkout`, then runs the cloned `scripts/verify_release.py --require-clean --evidence <fresh>/evidence`. No working-tree `.lake` products are copied, no Git object alternates are retained, and exact package HEADs/source cleanliness are checked before and after. Process-local controls are `LEAN_NUM_THREADS=2`, `LAKE_NO_CACHE=true`, `LAKE_ARTIFACT_CACHE=false`, `LAKE_CACHE_DIR=` and `MATHLIB_NO_CACHE_ON_UPDATE=1`, with inherited Lean library/source paths cleared. Two-thread concurrency was measured with actual pinned Lake consumers. The already installed Lean 4.31.0 toolchain is the fixed compiler; its exact commit is checked by the runner.

The final clean receipt, certified source revision and source-hash comparison will be appended after reconstruction succeeds. A current-checkout pass alone is not that certificate.
