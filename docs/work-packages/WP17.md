# WP17: Simplex conditional recursion and repeated-direction reference

- Status: integrated; independently reviewed and freshly audited
- Owner: root, sole owner of Recursion.lean and checks/wp17
- Dependencies: WP02, WP03, WP05
- Owned files: `FSC/Simplex/Recursion.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md reference recursion; sources/TEACHING_MANUSCRIPT.md chapter 9
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove simplex conditional law and derivative recursion; pad a nonempty retained list by repeating inequalities.

## Acceptance contract

D_n'(t)=n*phi(t)*D_(n-1)(t*sqrt(n/(n-2))); includes n=3 antipodal residual.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Root read teaching manuscript 9.1 and the reviewed dossier reference recursion after WP05 actual support C1 was accepted. Frozen consumer: the actual simplex cdf derivative at positive thresholds for n≥3 equals n*phi(t) times the lower-size simplex cdf at t*sqrt(n/(n−2)); the n=3 residual is antipodal and must remain admitted. The reference adapter repeats a nonempty retained unit-normal list to exactly n−1 entries without changing its event. WP21 worker owns the shared simplex correlation helper in Simplex/Definitions.lean; root owns no overlapping files.

Accepted declarations: `map_simplex_singleLaw` sends the actual canonical full-index pin law, by standardized deletion and the exact affine mean correction, to the lower-size regular Gaussian. `singlePinMass_simplex` evaluates its success event at every real t. `hasDerivAt_simplex_cdf` gives exactly `HasDerivAt (cdf (simplex n)) (n*normalPDF(t)*cdf(simplex(n−1))(t*sqrt(n/(n−2)))) t` for n≥3,t>0. The n=3 lower covariance is the actual antipodal simplex2, admitted without a PD assumption. `exists_correlation_capMass` extends any finite nonempty unit-normal list by a surjective repetition map, producing an actual correlation whose CDF equals the cap mass for every real threshold. `reference_floor_of_comparison` consumes precisely the lower-size all-correlation comparison, with no reference regularity or distinctness premise.

Verification at base82ee3ec: `lake build FSC.Simplex.Recursion --wfail` exited0; fresh required-endpoint audit on checks/wp17/Audit.lean passed14 declarations, saved checks/wp17/audit.jsonl. Fresh checks/wp17/Adverse.lean audit passed4, saved adverse-axioms.jsonl. All closures are subsets of propext/Classical.choice/Quot.sound. Adverses include the exact triangle recursion, actual antipodal conditional Gaussian law, equal-support repeated antipodal cap event, and its reference-floor consumer. No source was adapted from a private declaration.

Independent wp00_algebra review read the complete source and accepted covariance and mean standardization, succAbove deletion/event equivalence, positivity of sigma, exact threshold scale, and actual repetition law. It confirmed all-real pin identities and absence of reference tie exclusions. Failed guessed Fintype embedding APIs and actual compiler output are preserved in checks/wp17/API.lean and api.log; accepted repetition uses equivFin plus castLE and invFun. Under-typed finite values and an under-specified forall predicate failed before explicit types/predicates fixed them. The first adverse numeral simplification generated a compiler error axiom and was rejected; its JSON is retained separately as adverse-failed-numerals.jsonl. Explicit conversion and normalization produced the fresh clean receipt. No failure changed a mathematical interface.
