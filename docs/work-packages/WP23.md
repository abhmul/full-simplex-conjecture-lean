# WP23: Positive-threshold equality and strictness

- Status: paused at user request; compiled positive-contact helper only
- Owner: `/root/wp00_algebra`, assigned by lead before edits; root retains Git/integration
- Dependencies: WP19, WP21, WP22
- Owned files: `FSC/Equality.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; pro-return/DOSSIER.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

After comparison, differentiate the nonnegative fixed-shape difference at its positive zero; invoke slope rigidity.

## Acceptance contract

FSC.cdf_eq_simplex_iff and cdf_simplex_lt, including all ranks and exceptional configurations.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned this package after WP15 actual finite-direction source acceptance. The worker read the exact public equality/strictness interfaces, the public contract and manuscript §9.3. Ownership of `FSC/Equality.lean`, this card and `checks/wp23/` is recorded before edits. First consumer proves equality of actual boundary slopes at a positive contact using the already proved `slope_eq_of_touch`; this is independent of the remaining lower-size comparison assembly. Final equality will discharge comparison and the minimizer distinctness/singularity conditions internally, using the actual WP19 rigidity endpoint. The final public signature retains every correlation and only the intended positive-threshold assumption.

At the user's travel pause, `FSC.boundarySlope_eq_of_cdf_touch hn G hG hd t ht hcomp heq` is the only new source declaration. It proves `boundarySlope G t = boundarySlope (simplex n) t` for n≥2 using the actual `hasDerivAt_cdf` for both matrices, with `hcomp : ∀ s, 0 < s → cdf (simplex n) s ≤ cdf G s` and the contact equality. `lake env lean -DwarningAsError=true FSC/Equality.lean` exited 0. The helper is an ordinary theorem with explicit comparison/distinctness hypotheses; the final public equality and strictness endpoints are not implemented, and no fresh importing audit of this helper is claimed.

The frozen planned remaining consumer is the analyst's `eq_simplex_of_boundarySlope_eq_of_minimum hn G hG t ht hmin hcompare hslope`, where hn is n≥3, `hmin` quantifies over every same-size correlation, `hcompare` is lower-size comparison at `t / referenceRatio n`, and `hslope` equates the actual boundary slopes. After the lead's `cdf_simplex_le` is installed, derive `hmin` from the contact and all-correlation comparison, derive distinctness using `distinctScores_of_cdf_minimum`, derive `hslope` with the compiled helper, and discharge the lower-size comparison by the same public comparison theorem. Handle n=2 with the accepted `two_site_equality`; prove strictness from comparison plus the iff. No final-equality proof or placeholder import was started before the user pause. No worker compiler/audit process remains running.
