# WP23: Positive-threshold equality and strictness

- Status: reviewed
- Owner: `/root/wp00_algebra`, assigned by lead before edits; root retains Git/integration
- Dependencies: WP19, WP21, WP22
- Owned files: `FSC/Equality.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; pro-return/DOSSIER.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

After comparison, differentiate the nonnegative fixed-shape difference at its positive zero; invoke slope rigidity.

## Acceptance contract

FSC.cdf_eq_simplex_iff and cdf_simplex_lt, including all ranks and exceptional configurations.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned this package after WP15 actual finite-direction source acceptance. The worker read the exact public equality/strictness interfaces, the public contract and manuscript §9.3. Ownership of `FSC/Equality.lean`, this card and `checks/wp23/` is recorded before edits. First consumer proves equality of actual boundary slopes at a positive contact using the already proved `slope_eq_of_touch`; this is independent of the remaining lower-size comparison assembly. Final equality will discharge comparison and the minimizer distinctness/singularity conditions internally, using the actual WP19 rigidity endpoint. The final public signature retains every correlation and only the intended positive-threshold assumption.

At the user's travel pause, `FSC.boundarySlope_eq_of_cdf_touch hn G hG hd t ht hcomp heq` is the only new source declaration. It proves `boundarySlope G t = boundarySlope (simplex n) t` for n≥2 using the actual `hasDerivAt_cdf` for both matrices, with `hcomp : ∀ s, 0 < s → cdf (simplex n) s ≤ cdf G s` and the contact equality. `lake env lean -DwarningAsError=true FSC/Equality.lean` exited 0. The helper is an ordinary theorem with explicit comparison/distinctness hypotheses; the final public equality and strictness endpoints are not implemented, and no fresh importing audit of this helper is claimed.

The frozen planned remaining consumer is the analyst's `eq_simplex_of_boundarySlope_eq_of_minimum hn G hG t ht hmin hcompare hslope`, where hn is n≥3, `hmin` quantifies over every same-size correlation, `hcompare` is lower-size comparison at `t / referenceRatio n`, and `hslope` equates the actual boundary slopes. After the lead's `cdf_simplex_le` is installed, derive `hmin` from the contact and all-correlation comparison, derive distinctness using `distinctScores_of_cdf_minimum`, derive `hslope` with the compiled helper, and discharge the lower-size comparison by the same public comparison theorem. Handle n=2 with the accepted `two_site_equality`; prove strictness from comparison plus the iff. No final-equality proof or placeholder import was started before the user pause. No worker compiler/audit process remains running.

The user explicitly resumed at clean integration HEAD `c5132e4`. The same worker resumed the same ownership and independently reviewed the entire then-current WP19 core before completing the equality consumer. The review found the reciprocal tangent and reference-floor denominator orientation correct, strict physical energy supplied to componentwise equality, and the exact uniform-row route to full matrix rigidity. No reference tie differentiability or lower-size uniqueness enters. The core's explicit row tests/reference floor remain genuine hypotheses to be discharged by its minimum wrapper and the all-dimensional comparison theorem.

## Accepted public endpoints

`FSC.cdf_eq_simplex_iff {n} (hn : 2 ≤ n) (G : Mat n) (hG : WeakSimplex.IsCorrelation G) (t : ℝ) (ht : 0 < t)` proves exactly `cdf G t = cdf (simplex n) t ↔ G = simplex n`. `FSC.cdf_simplex_lt` has precisely those hypotheses plus `hne : G ≠ simplex n` and proves `cdf (simplex n) t < cdf G t`. Neither final endpoint has distinctness, singularity, rank, balance, lower-size comparison, derivative or certificate hypotheses.

The n=2 branch consumes the accepted actual two-site iff. For n≥3, the proved all-correlation comparison and the assumed contact give an actual global minimum. The accepted exclusion derives distinctness, so the actual CDF derivative and positive-threshold contact theorem apply. `eq_simplex_of_boundarySlope_eq_of_minimum` then consumes the resulting equal slopes and the proved all-dimensional comparison instantiated at size n−1. Thus both the all-threshold comparison and every internal minimizer/slope hypothesis are discharged. Strictness follows from comparison and the iff with the equality orientation checked.

`lake env lean -DwarningAsError=true FSC/Equality.lean` exited 0. The lead's `lake build FSC.Equality --wfail` exited 0 (`checks/equality-install.log`, target reported 2.2 seconds). Both the lead and the analytic worker independently read the complete final source and accepted its hypothesis discharge, signs and exact public types. Final source SHA256 is `b9359b1a8630148cc14e3913bcad313a0ed33564112355361edb038eabff81df`; the first completed receipt was against integration HEAD `c5132e4e28f112d703757c531b310d3d22861371` before root serial integration.

Fresh `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp23/Audit.lean --required FSC.cdf_eq_simplex_iff --required FSC.cdf_simplex_lt --save-json checks/wp23/axioms.jsonl` exited 0 for all three production endpoints, including the contact helper. The audit prints fully expanded Lean types. Every transitive axiom is in `propext`, `Classical.choice`, `Quot.sound`; there is no `sorryAx`, native-computation or project axiom.

## Adverse consumers and compiler evidence

`checks/wp23/Adverse.lean` calls the closed public theorem on the full-rank identity matrix of size three, the rank-one Gram of `(1,-1,1)` with both duplicate and antipodal scores, and the singular four-site square Gram. Each concrete matrix's actual PSD/unit-diagonal and nonsimplex properties are proved. It also checks equality for the regular simplex in every n≥2 and the public two-site iff reformulated exactly as `G 0 1 = -1`. The fresh audit command on `Adverse.lean`, requiring its identity, signed-Gram, square-Gram strictness and two-site equality endpoints, passed all five printed declarations; `adverse-axioms.jsonl` contains only the permitted three axioms. The already accepted WP21 actual-law counterexample continues to show why the positive-threshold restriction cannot be dropped from equality: `(Z,-Z,Z)` equals the simplex CDF at every nonpositive threshold despite its nonsimplex matrix.

The final equality proof compiled without failed proof attempts. The witness PSD proof needed explicit `signedGram` unfolding, and a finite numeral entry required changing the remaining definitional expression to `(1 : ℝ) = -(1/2)` before linear arithmetic. While root completed Comparison, a bounded fixed-CDF continuity consumer reproduced its elaboration timeout. `checks/wp23/ContinuityProbe.lean` preserves a compiled repair: explicitly name the inner and outer functions of `Continuous.comp` and change the target to their composition. The source declaration uses `g` for the outer function and `f` for the inner; the initially reversed guessed names produced an application-type mismatch. The repaired probe compiles warning-clean at default heartbeats. No continuity or mathematical-contract change was needed.

WP23 is complete locally with independent source review and fresh installed-artifact audits. Public-root/full builds, expanded release statement review, all required-endpoint transitive auditing and clean-checkout reproduction remain the lead's separate final acceptance gates. Publication and external acceptance remain separate operator decisions.
