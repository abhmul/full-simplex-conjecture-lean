# WP24: Measure and finite-maximum public wrappers

- Status: reviewed
- Owner: `/root/wp00_imports`, assigned by lead before edits
- Dependencies: WP22, WP23
- Owned files: `FSC/Main.lean`, `FSC/Gaussian/Maximum.lean`, `checks/wp24/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; pro-return/INTERFACES.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove real/event conversion and finite-maximum complement statement; review exact direction.

## Acceptance contract

FSC.lowerOrthant_simplex_le and exact maximum-tail theorem; no MGF substitution.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned ownership after WP16. Read `docs/PUBLIC_CONTRACT.md` and the actual `docs/pro-return/INTERFACES.md`; the initially guessed `docs/INTERFACES.md` path does not exist. Exact local source discovery found the public `WeakSimplex.coordinateMax`, `continuous_coordinateMax` and `coordinateMax_le_iff_mem_lowerOrthant` in pinned `WeakSimplexConjectureLean/Maxima/CoordinateMax.lean`. The maximum-tail direction is `Pr_G(t < max) ≤ Pr_simplex(t < max)` because the corresponding lower-orthant comparison goes in the reverse direction. Existing WSC stochastic-order theorems assume `IsWeakSimplexCov` and are not FSC replacements; this package consumes only the public maximum/event dictionary and ordinary probability complementation.

The generic exact dictionary and complement adapters will compile before the final comparison/equality inputs are available. `FSC/Main.lean` will contain closed public consumers only after WP22 and WP23 are installed; no placeholder import, axiom or conditional FSC release theorem is introduced.

## Travel pause checkpoint

`FSC/Gaussian/Maximum.lean` is authored and its direct `lake env lean -DwarningAsError=true FSC/Gaussian/Maximum.lean` returned exit 0 with no diagnostics. Source is stable. It exports ten exact generic theorems: the maximum lower-event dictionary, measurable lower event, strict upper-tail complement, `cdf_eq_probability_coordinateMax_le`, ENNReal and real tail complements, and event/real CDF comparison plus tail comparison/strictness/equality equivalences. These have no correlation/PSD assumptions because they are exact event and finite-probability identities for the total Gaussian constructor. The tail inequality equivalence explicitly reverses the CDF comparison direction.

The lead received a user travel-pause request immediately after this successful direct compilation. No Lake target install or fresh transitive audit has been claimed for this module. `FSC/Main.lean` has not been created. Resume by installing `FSC.Gaussian.Maximum` serially, writing/importing its exact endpoint audit, then consuming the closed WP22 comparison and WP23 equality in `FSC/Main.lean` when available. No running Maximum compiler remains.

## Resumed maximum dictionary receipt

The user explicitly resumed and the lead installed `FSC.Gaussian.Maximum` with `--wfail`, exit 0, then independently reviewed the entire source and the complement reversal. The fresh importing ten-endpoint audit passed: `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp24/MaximumAudit.lean --required ENDPOINT ... --save-json checks/wp24/maximum-axioms.jsonl`; the importing file lists all ten required names. Every reported axiom is in the allowed three-element set. The final wrapper names are now frozen with the lead: `lowerOrthant_simplex_le`, `coordinateMax_tail_le_simplex`, `coordinateMax_tail_lt_simplex`, `coordinateMax_tail_eq_simplex_iff`. Final source awaits closed WP22/WP23 upstream declarations.

While those declarations compile independently, the lead requested a read-only WP25 environment review. The exact source-build and pin-verification proposal is `checks/wp24/CLEAN_REPRODUCTION_PLAN.md`; it distinguishes source-known command interfaces from unexecuted acceptance gates and records both cache-disabling controls and the tracked ProofWidgets JavaScript assets. No clean reproduction or final acceptance is claimed by that preparation.

The concrete maximum dictionary adverse file `checks/wp24/Adverse.lean` compiled directly warning-clean and passed a fresh required five-endpoint audit saved to `checks/wp24/adverse-axioms.jsonl`. At zero covariance the strict upper tail at threshold zero is exactly zero, while the threshold-minus-one tail is exactly one; this tests complement behavior in the presence of an atom. Every simplex with `n≥2` has tail one at nonpositive threshold. The existing all-real two-site comparison and strict-positive two-site theorem produce the correct reversed tail inequalities. These are actual probability consumers; the zero covariance tests intentionally exercise the generic dictionary outside the correlation domain.

## Closed public wrappers

After WP22 comparison and WP23 equality were installed, `FSC/Main.lean` compiled directly with `lake env lean -DwarningAsError=true FSC/Main.lean`, exit 0. The lead installed `lake build FSC.Main --wfail`, exit 0 (`checks/main-install.log`), and independently reviewed all four wrapper sources and the maximum dictionary. The final source is stable and imports only `FSC.Equality` and `FSC.Gaussian.Maximum`.

Exact accepted interfaces:

- `lowerOrthant_simplex_le {n} (hn : 2 ≤ n) G (hG : WeakSimplex.IsCorrelation G) (t : ℝ)` proves the literal Gaussian lower-orthant measure at the simplex is at most that for G, at every real t.
- `coordinateMax_tail_le_simplex hn G hG t` proves the literal event measure `{x | t < WeakSimplex.coordinateMax (by omega : 0 < n) x}` for G is at most the simplex measure, at every real t.
- `coordinateMax_tail_lt_simplex hn G hG t (ht : 0 < t) (hne : G ≠ simplex n)` proves strict inequality in that same tail direction.
- `coordinateMax_tail_eq_simplex_iff hn G hG t (ht : 0 < t)` proves equality of those literal tail measures iff `G = simplex n`.

No distinctness, singularity, rank, normalization, hidden analytic certificate or positive-threshold premise occurs in either comparison wrapper. Equality and strictness retain exactly the fixed-positive-threshold contract. Final importing fresh audit source is `checks/wp24/MainAudit.lean`; its exact receipt follows below. These local wrappers do not by themselves claim WP25 expanded-statement/full-build/clean-checkout acceptance, publication or external acceptance.

The fresh required four-endpoint Main audit passed, exit 0, saved to `checks/wp24/main-axioms.jsonl`. The executed command was `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp24/MainAudit.lean --required FSC.lowerOrthant_simplex_le --required FSC.coordinateMax_tail_le_simplex --required FSC.coordinateMax_tail_lt_simplex --required FSC.coordinateMax_tail_eq_simplex_iff --save-json checks/wp24/main-axioms.jsonl`. Each closure is exactly `[propext, Classical.choice, Quot.sound]`; the file also prints all four type signatures. `git diff --check` on all owned source/check/card paths returned exit 0. Root and the independently assigned WP25 statement reviewer were notified that Main is installed and stable.

The lead also requested a local concurrency probe for the final source rebuild. The self-contained, dependency-free Lake project under `checks/wp24/concurrency/` is execution-only infrastructure; none of its `#eval` commands is imported by FSC or used to prove a theorem. Fresh no-cache builds demonstrated maximum compiler-work overlap one under `LEAN_NUM_THREADS=1` and two under `LEAN_NUM_THREADS=2`; raw logs and exact intervals are preserved in the clean-reproduction plan. The initial `BaseIO`/`IO` diagnostic is preserved there as well. No global configuration, dependency pin, external repository, remote or new-work license was changed.
