# WP24: Measure and finite-maximum public wrappers

- Status: in-progress
- Owner: `/root/wp00_imports`, assigned by lead before edits
- Dependencies: WP22, WP23
- Owned files: `FSC/Main.lean`, `FSC/Gaussian/Maximum.lean`, `checks/wp24/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; pro-return/INTERFACES.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove real/event conversion and finite-maximum complement statement; review exact direction.

## Acceptance contract

FSC.lowerOrthant_simplex_le and exact maximum-tail theorem; no MGF substitution.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned ownership after WP16. Read `docs/PUBLIC_CONTRACT.md` and the actual `docs/pro-return/INTERFACES.md`; the initially guessed `docs/INTERFACES.md` path does not exist. Exact local source discovery found the public `WeakSimplex.coordinateMax`, `continuous_coordinateMax` and `coordinateMax_le_iff_mem_lowerOrthant` in pinned `WeakSimplexConjectureLean/Maxima/CoordinateMax.lean`. The maximum-tail direction is `Pr_G(t < max) ≤ Pr_simplex(t < max)` because the corresponding lower-orthant comparison goes in the reverse direction. Existing WSC stochastic-order theorems assume `IsWeakSimplexCov` and are not FSC replacements; this package consumes only the public maximum/event dictionary and ordinary probability complementation.

The generic exact dictionary and complement adapters will compile before the final comparison/equality inputs are available. `FSC/Main.lean` will contain closed public consumers only after WP22 and WP23 are installed; no placeholder import, axiom or conditional FSC release theorem is introduced.

## Travel pause checkpoint

`FSC/Gaussian/Maximum.lean` is authored and its direct `lake env lean -DwarningAsError=true FSC/Gaussian/Maximum.lean` returned exit 0 with no diagnostics. Source is stable. It exports ten exact generic theorems: the maximum lower-event dictionary, measurable lower event, strict upper-tail complement, `cdf_eq_probability_coordinateMax_le`, ENNReal and real tail complements, and event/real CDF comparison plus tail comparison/strictness/equality equivalences. These have no correlation/PSD assumptions because they are exact event and finite-probability identities for the total Gaussian constructor. The tail inequality equivalence explicitly reverses the CDF comparison direction.

The lead received a user travel-pause request immediately after this successful direct compilation. No Lake target install or fresh transitive audit has been claimed for this module. `FSC/Main.lean` has not been created. Resume by installing `FSC.Gaussian.Maximum` serially, writing/importing its exact endpoint audit, then consuming the closed WP22 comparison and WP23 equality in `FSC/Main.lean` when available. No running Maximum compiler remains.
