# WP01: Public definitions and coupled interface freeze

- Status: integrated
- Owner: root, coupled definitions and integration
- Dependencies: WP00
- Owned files: `FSC/Definitions.lean`, `FSC/Gaussian/PinDefinitions.lean`, `FSC/Support/Definitions.lean`, `FSC/Analysis/Definitions.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; pro-return/INTERFACES.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Freeze transparent correlation/CDF/simplex aliases and a single full-index pin/event/q/stress convention; finite support definitions and the shared Peano2 predicate. Do not introduce proof oracles. WP07 proves the expansion; WP08 consumes the already frozen predicate independently.

## Acceptance contract

Definitions elaborate; exact printed contracts and normalization reviewed; basic probability/event measurability wrappers.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

2026-09-08: all four shared definition files elaborate warning-clean. `lake build FSC.Definitions FSC.Gaussian.PinDefinitions FSC.Support.Definitions --wfail` exited 0; `FSC.Analysis.Definitions` was built independently with exit 0. The exact reviewed convention is a full-index congruence pair covariance, explicit real pin inputs, remaining-coordinate events excluding pins, one piecewise q, stress/rowK/rowC/kappa, ambient-dimension capEnergy, explicit affine sliceLaw and continuous-linear supportGradient. `Peano2` is vector-domain little-o of squared norm at zero with the factor 1/2 and no analytic certificate. The independent triangle worker compared all conventions to the reviewed source and found no mathematical mismatch.

Accepted basic endpoints: `FSC.cdf_eq_measure`, `cdf_nonneg`, `cdf_le_one`, `thresholdCDF_const`, `simplex_apply`, `simplex_eq_scaled_matrix`, `measurableSet_thresholdEvent`, `measurableSet_singleEvent`, `measurableSet_pairEvent`, `measurableSet_cap`, `q_self`. `simplex_eq_scaled_matrix` explicitly identifies the WSC alias with `(1 / ((n : ℝ) - 1)) • ((n : ℝ) • 1 - J)` under `2 ≤ n`. Fresh `checks/WP01Audit.lean` prints expanded public bridges, unfolds `WeakSimplex.IsCorrelation`, and prints the coupled definitions. `scripts/audit_axioms.py` accepted all 11 endpoints (ordinary axiom subset only), output `checks/wp01-axioms.jsonl`.

Compiler corrections preserved: simplex lives in WSC `Coding.Gram`, not Core.Matrix; probability bound API is `prob_le_one`, not `measure_le_one`; measurable inner was proved through `(continuous_const.inner continuous_id).measurable`; rational simplex branches use field_simp followed by ring only on the remaining branch, avoiding the unnecessary sequence-focus warning. No mathematical assumption changed. These definitions have no Gaussian comparison or derivative theorem; WP02–WP09 and all final consumers still owe their proofs.
