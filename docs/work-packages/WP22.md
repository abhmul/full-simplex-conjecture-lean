# WP22: All-dimensional comparison

- Status: integrated; independently reviewed and freshly audited
- Owner: root, sole owner of Comparison.lean, this card and checks/wp22
- Dependencies: WP13, WP19, WP20, WP21
- Owned files: `FSC/Comparison.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; sources/CORE_PROOF.md section 5
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Induct using lower-size comparison and discharge all actual analytic/barrier inputs.

## Acceptance contract

FSC.cdf_simplex_le for all n>=2,G correlation,t real; no hidden certificate.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Root resumed this package from clean c5132e4, read the exact public contract, accepted compact barrier/continuity/base case statements and the dossier final barrier section. Frozen final interface is exactly `cdf_simplex_le hn G hG t`, n≥2, arbitrary correlation G and every real t. Induction supplies only the lower-size comparison to the actual minimum-slope wrapper; all compactness, continuity, derivative and endpoint inputs must be discharged explicitly. WP19 worker owns the slope wrapper and will announce stable installation before this importing consumer is compiled.

Accepted declarations: `FSC.cdf_simplex_le_of_lower` is a genuine induction-step helper whose only extra mathematical input is lower-size comparison. `FSC.cdf_simplex_le {n} (hn : 2 ≤ n) (G : Mat n) (hG : WeakSimplex.IsCorrelation G) (t : ℝ) : cdf (simplex n) t ≤ cdf G t` discharges this input by strong induction, with the actual two-site and nonpositive-threshold branches. No equality induction, distinctness, rank, positivity or analytic-certificate input remains.

Commands: `lake env lean -DwarningAsError=true FSC/Comparison.lean` and `lake build FSC.Comparison --wfail` exited 0; logs are checks/wp22-comparison-build.log and checks/wp22-comparison-install.log. Fresh `scripts/audit_axioms.py checks/wp22/ComparisonAudit.lean --required FSC.cdf_simplex_le --save-json checks/wp22/comparison-axioms.jsonl` exited 0 for four declarations, all using only propext/Classical.choice/Quot.sound. The additional importing consumers admit the rank-one all-duplicate covariance at every real threshold and the full two-site domain. All three workers independently read the induction/barrier implementation and accepted the exact statement and hypothesis discharge; the WP25 reviewer also compiled a literal Gaussian-event consumer with PSD and diagonal hypotheses separated.

Failed experiments retained: first compile reached a deterministic 200000-heartbeat elaboration limit in a composed continuity expression and exposed the missing `generalizing t` in strong induction. A named typed composition followed by `simpa only [Function.comp_def]` resolved the former without increasing heartbeats. The all-ones adverse consumer could not treat WSC's private allOnesMatrix_posSemidef as public. Two actual simp-only attempts failed to identify the matrix under PosSemidef; a separately proved entrywise matrix identity and the accepted rankOne_posSemidef adapter succeeded. Failure logs are explicitly named failed and are not acceptance reports; the final fresh audit contains no error axiom.
