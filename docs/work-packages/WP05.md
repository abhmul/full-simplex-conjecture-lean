# WP05: Finite Gaussian support C1 neighborhoods

- Status: reviewed; production and adverse importing audits passed
- Owner: `/root/wp00_imports`, assigned by implementation lead on 2026-09-08 before edits
- Dependencies: WP03, WP26
- Owned files: `FSC/Support/Differentiation.lean`, `FSC/Support/Slicing.lean`, `checks/wp05-support/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/SUPPORT_NOISE_VARIATION.md section 2; sources/TEACHING_MANUSCRIPT.md chapter 5
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Slicing plus dominated convergence gives continuously varying partials and a Frechet derivative on a no-coincidence neighborhood.

## Acceptance contract

Neighborhood C1 with explicit slice gradient; redundant parallel constraints, empty index families and unbounded caps covered.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned exclusive ownership of `FSC/Support/Differentiation.lean`, this card and `checks/wp05-support/` to `/root/wp00_imports`. Generic coordinate calculus is installed separately as WP26; it is not an acceptance certificate for this Gaussian support package. Read `SUPPORT_NOISE_VARIATION.md` section 2, actual frozen `capMass`, `sliceLaw`, `sliceMass`, `NoCoincident`, and the compiled `SinglePin` product-law/Fubini endpoints before proof edits. The planned accepted statement uses unit normals, actual `NoCoincident w b`, and derives a neighborhood derivative equal to the frozen `supportGradient w b`, with finite empty families, parallel redundancy and unbounded caps retained.

The lead explicitly assigned the helper `FSC/Support/Slicing.lean` before its edits. It factors the standard Gaussian into its projection onto a unit normal and the explicit orthogonal residual in the original ambient space. All real pin values are interpreted using the frozen actual `sliceLaw`; no conditional law at a null event is chosen.

## Exact accepted statements

```lean
FSC.hasFDerivAt_capMass
    {d : ℕ} {ι : Type*} [Fintype ι] [DecidableEq ι]
    (w : ι → Coord d) (hw : ∀ k, ‖w k‖ = 1)
    (b : ι → ℝ) (hb : NoCoincident w b) :
    HasFDerivAt (capMass w) (supportGradient w b) b

FSC.capMass_support_C1
    {d : ℕ} {ι : Type*} [Fintype ι] [DecidableEq ι]
    (w : ι → Coord d) (hw : ∀ k, ‖w k‖ = 1)
    (b : ι → ℝ) (hb : NoCoincident w b) :
    ∃ U : Set (ι → ℝ), IsOpen U ∧ b ∈ U ∧ ContDiffOn ℝ 1 (capMass w) U ∧
      ∀ b' ∈ U, HasFDerivAt (capMass w) (supportGradient w b') b'
```

The chosen neighborhood is exactly the open set of noncoincident listed boundaries. `FSC.contDiffAt_one_capMass` supplies the local C1 form for composition. `FSC.hasDerivAt_capMass_update` identifies each scalar slice derivative as `normalPDF (b j) * sliceMass w b j`. `FSC.continuousAt_sliceMass` proves continuity of the actual slice probability at every noncoincident support. All unit-normal and noncoincidence hypotheses are explicit; there is no assumed regularity field, full-rank hypothesis, positive dimension, irredundancy, bounded cap or positive support requirement. Empty families in dimension zero are allowed.

The exact Fubini statements `FSC.capMass_slicing_gaussian` and `FSC.capMass_slicing` hold for every support vector, including ties. The latter is:

```lean
capMass w b =
  ∫ z in Set.Iic (b j), normalPDF z * sliceMass w (Function.update b j z) j
```

The scalar FTC uses integrability of the entire density times bounded slice probability and continuity only at the actual point. It imposes no continuity at a tied reference elsewhere.

## Mathematical construction and reused interfaces

Read the actual teaching manuscript §§5.1 and 5.4 as well as support/noise section 2. `normalResidual w = id - (innerSL ℝ w).smulRight w` retains the original ambient dimension. `map_inner_stdGaussian` proves the scalar Gaussian law including a zero normal from actual Gaussianity, mean and variance. `normalResidual_indep_inner` first proves joint Gaussianity, then computes zero cross covariance via `covarianceBilin_apply_eq_cov` and `covarianceBilin_stdGaussian`. Product-law factorization yields `lintegral_normalSlice` for arbitrary measurable tests of both vector and real pin. The frozen `sliceLaw` is proved equal to translated residual pushforward at every real pin.

For continuity, `slicePreimage` is the explicit residual-sample preimage of the unpinned constraints. When a residual functional is nonzero, `ae_inner_ne` proves every real boundary is null using its actual nondegenerate Gaussian scalar law. When it vanishes, unit norms force equal or opposite normals; the exact `NoCoincident` condition makes the corresponding deterministic boundary strictly separated. Thus finitely many indicator constraints stabilize almost surely under support convergence. The public pinned `tendsto_measure_of_ae_tendsto_indicator_of_isFiniteMeasure` supplies dominated convergence. The resulting continuous partial derivatives feed the separately audited finite-input adapter WP26.

The density conversion uses public `integral_gaussianReal_eq_integral_smul`; `integral_toReal` is applied only after measurability and finiteness of the bounded scalar integrand are proved. The moving-halfline FTC is an original local-continuity wrapper around pinned `intervalIntegral.integral_hasDerivAt_right` and `intervalIntegral.integral_Iic_sub_Iic`. Its structure follows the already compiled triangle consumer, with the unnecessary global continuity premise removed using actual integrability. No external private source slice was copied for WP05.

## Verification

`lake env lean -DwarningAsError=true FSC/Support/Slicing.lean` and the analogous command for `FSC/Support/Differentiation.lean` each exited 0. The lead serially installed both Lake targets with `--wfail`, exit 0. Fresh importing required-endpoint audits passed nine slicing declarations and eleven differentiation declarations. Every printed closure is exactly `[propext, Classical.choice, Quot.sound]`. Sources and actual JSON diagnostics are `checks/wp05-support/SlicingAudit.lean`, `slicing-axioms.jsonl`, `Audit.lean`, `axioms.jsonl`.

Reproduction uses `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py` on those two audit source files, with every corresponding `#print axioms` endpoint supplied by repeated `--required` and the named JSON output passed via `--save-json`. The required differentiation endpoints are `measurable_sliceMass`, `NoCoincident.eventually`, `ae_slice_boundary_ne`, `continuousAt_sliceMass`, `capMass_slicing_gaussian`, `capMass_slicing`, `integrable_normalPDF_mul_sliceMass`, `hasDerivAt_capMass_update`, `hasFDerivAt_capMass`, `contDiffAt_one_capMass`, `capMass_support_C1`, all under `FSC`.

## Failed compiler/API experiments

The covariance bilinear rewrite hit the pinned real star-linear versus linear instance transparency mismatch. The proof now names a scalar covariance equality and transfers it with `simpa ... using!`, avoiding guessed declarations or changed mathematics. The initial `ae_map_iff` call supplied measurability where the actual API requires a.e. measurability, and the guessed `measurableSet_ne_fun` does not exist; the accepted proof uses `.measurable.aemeasurable` and the complement of the actual equality-measurable set. Indicator simplification at heterogeneous products needed explicit membership cases rather than a guessed definitional `ite` representation. `ContinuousAt.comp` names its inner function `f`, not `g`; exact argument annotations resolved its initially incorrect inference of a topology on the index type. All failed attempts were ordinary API/elaboration friction; no mathematical architecture or trust change was needed.

## Next critical consumer

WP07 must use the actual Gram/projected normals and prove their unit norms and noncoincidence at the positive common support, identify the actual canonical one-/two-pin laws with the cap slices, differentiate the resulting gradient and identify every Hessian entry with the canonical pair weights. The universal support theorem is now available; it does not itself identify those Gaussian regression laws or establish the covariance derivative.

## Independent review and adverse acceptance

The lead independently read both complete source files and reviewed joint Gaussian independence, arbitrary-test Fubini, finite indicator stabilization, deterministic parallel/antiparallel cases, openness, local FTC and the exact common neighborhood. The review found no leaked positivity, rank or boundedness hypothesis and accepted the mathematical statements.

`lake env lean -DwarningAsError=true checks/wp05-support/Adverse.lean` exited 0. A fresh audit requiring all seven printed endpoints passed, each using `[propext, Classical.choice, Quot.sound]`; output is `checks/wp05-support/adverse-axioms.jsonl`. The compiled consumers cover the manuscript's original five planar normals at every positive common threshold; the projected `[+1,+1,-1]` normals at actual supports `[t,t/2,t]`; zero derivative of the weaker upper constraint, derived from its actual Dirac slice law; empty families in ambient dimension zero; the unbounded redundant triangle cap; the proved failure of `NoCoincident` for opposite normals at zero support; and the existing exact tied-reference nondifferentiability counterexample. The n5 test fixes the stated geometric data and applies the universal support theorem; the canonical regression bridge identifying those data from a pin remains WP07/WP16 work.

The final adverse compiler repair replaced an invalid definitional identification of equivalent `Fin 1` constant-vector encodings by explicit extensional equality. No acceptance hypothesis changed. The worker also independently reviewed the lead's full `Support/Dilation.lean` and `Support/Padding.lean` files: the actual product Gaussian law, full Euclidean sum-of-squares identity and `k * capMass` energy correction are mathematically sound; no product supremum-norm substitution or bounded-cap premise was found. This review was reported directly to the lead for those separately owned cards.
