# WP15: Finite row tests and signed loss rigidity

- Status: reviewed
- Owner: `/root/wp00_algebra`, assigned by lead before edits; root retains Git/integration
- Dependencies: WP04, WP09
- Owned files: `FSC/Minimizers/FiniteDirections.lean`, `FSC/Scalar/RowLoss.lean`, this card and `checks/wp15/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md Pair weights and finite tests
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Freeze z_i at the base minimizer; derive row Cauchy, exact nonnegative signed-row remainder and positive-edge equality.

## Acceptance contract

Sii>=ki^2/kappa and signed-row inequality/equality; no assumption S PSD or GS=0.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned this package after WP21 source acceptance and the fresh base-case production audit. The worker has read `DOSSIER.md` finite row tests and equality paragraphs and is reading the full corresponding mathematical sections before proof work. Ownership is recorded before edits. The finite tests use the same canonical stress/q, freeze each test vector at the base minimizer and use one-sided feasible derivatives. No PSD stress or GS=0 assumption is permitted. The exact universal-variation endpoint is being coordinated with the analytic worker; independent matrix and signed-row algebra can proceed against genuinely explicit derivative hypotheses, with the actual consumer to discharge them once installed.

## Accepted declarations and interfaces

`FSC/Scalar/RowLoss.lean` defines the single conventions `rowWeight = rowK/kappa`, `rowRatio = rowC/rowK`, and `referenceRatio n = sqrt((n-2)/n)`. Its 25 proved endpoints include the actual canonical weighted Cauchy inequality, its exact sum-of-squares remainder, the exact signed-row remainder, signed inequality and equality, positive row weights and ratios, and positive-edge rigidity. `correlation_eq_simplex_of_uniform_rows` takes the actual row test bounds and uniform weights/reference ratios and proves the entire matrix is the simplex. It uses actual row-maximal q positivity to bound every off-diagonal entry and PSD positivity of the total Gram sum to force entrywise equality. No positivity assumption is imposed on every pair weight.

`FSC/Minimizers/FiniteDirections.lean` proves the stress row/column/total sums, `trace_stress_mul_self = 0`, `trace_stress_mul_allOnes = kappa`, and the exact quadratic form of `Pi.single i 1 - a*1`. `finiteTestVector` freezes `a = rowWeight G t i` at the base point. `trace_stress_finiteTest` computes its rank-one trace as `stress G t i i - rowK G t i ^ 2 / kappa G t`.

The actual analytic consumer is `half_trace_nonneg_of_minimum G L hG hd hL t ht hMin`, where `hMin : ∀ H : Mat n, IsCorrelation H → cdf G t ≤ cdf H t`. It applies the installed, fully discharged `hasDerivWithinAt_normalizedAdd`, the actual normalized PSD-addition feasibility theorem, and the elementary right-slope sign theorem. `finite_row_test_bound hn G hG hd t ht hMin i` then proves exactly `rowK G t i ^ 2 / kappa G t ≤ stress G t i i`, with no analytic certificate, fixed-rank constraint, stress PSD, balance or kernel condition. The n≥3, distinctness and positive-threshold assumptions remain internal to these minimizer consumers.

The lead independently reviewed every line of both modules and accepted the exact square remainder/equality orientation, global positive-edge rigidity, actual one-sided derivative sign, feasible PSD path and frozen direction. The lead's `lake build FSC.Scalar.RowLoss --wfail` and `lake build FSC.Minimizers.FiniteDirections --wfail` both exited 0 after worker `lake env lean -DwarningAsError=true PATH` compiled each source. Fresh importing audits passed for all 25 RowLoss and all 14 FiniteDirections endpoints. `checks/wp15/RowAudit.lean` and `FiniteAudit.lean` print the expanded load-bearing statements, with JSON receipts `row-audit.jsonl` and `finite-audit.jsonl`; every reported transitive axiom is in `propext`, `Classical.choice`, `Quot.sound`.

At these receipts, integration HEAD was `82ee3eced798c7b36e4f174b9e6fb3b9d09f5cb9`; source SHA256 values were RowLoss `257ec9c3e109bb699502adc262f266e815b22fce7aac53f81a18d6f01883557c` and FiniteDirections `fad16273d9bfbc67a01a903f9733d0b9603b9f5d0840532e4a9bcba06ce4772e`. Root owns subsequent coherent integration; workers made no commits. No pin or dependency change occurred.

## Preserved compiler/API evidence

The public derivative-to-slope API is `hasDerivWithinAt_iff_tendsto_slope'` after restricting from `Ici 0` to `Ioi 0`. The initially guessed `le_of_tendsto` proves an upper bound on the limit; the needed lower bound is `ge_of_tendsto`. The production proof uses that exact public theorem and positive slope quotients.

An untyped `∀ H` in a global-minimizer premise leaves matrix size unconstrained because `cdf G t ≤ cdf H t` alone does not identify its two index cardinalities. All accepted signatures explicitly bind `H : Mat n`. An all-ones function in a matrix product initially selected function-level multiplication; `Matrix.of (fun _ _ ↦ (1 : ℝ))` is the accepted matrix representation. A monolithic rewrite of the constant-vector quadratic form exposed a semireducible matrix column, then failed to match row/column sum lemmas. The read-only LSP helper returned the actual unsolved sum expression; an explicit `change` to that finite-sum expression allowed the exact existing row/column identities to close the goal. These are API/elaboration repairs, with no mathematical-interface changes.

In RowLoss, bare `simp only [stress, if_pos rfl]` retained `if True`; the exact simplifier lemma is `if_true`. Nested division in positive-edge simplex identification required an explicit proof that `(n-2)/n+1` is nonzero before `div_eq_div_iff`, not a change in the ratio convention. The actual signed remainder was preserved rather than weakened to an absolute-value loss. The audit CLI requires one repeated `--required NAME` flag per endpoint; its first grouped invocation was rejected by argparse and was rerun correctly before either accepted audit.

## Adverse acceptance

Fresh `checks/wp15/Adverse.lean` auditing passed all five declarations, with output `adverse-audit.jsonl` and only the permitted three axioms. The explicit valid row `n=3`, `a=1/8`, `A=sqrt(3/4)` has a strictly negative affine signed deficit, yet obeys the exact signed inequality. Saturating its covariance bound does not give signed-loss equality. The uniform row does give equality. The actual antipodal two-site law has `kappa=0`, preserving the dimension/positive-row guard before division. Finally the scalar path `s ↦ s` tests a positive right derivative at a right minimum. Its first numeric normalization attempt let `convert` descend into unrelated operator syntax; reducing the fixed real constants in a named inequality and using linear arithmetic closed the unchanged claim. Failed compilations were not accepted audit receipts.

Final commands were `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp15/RowAudit.lean --required FSC.row_cauchy_remainder --required FSC.signed_row_equality --required FSC.correlation_eq_simplex_of_uniform_rows --save-json checks/wp15/row-audit.jsonl`, the same script on `FiniteAudit.lean` requiring `FSC.finite_row_test_bound` and `FSC.half_trace_nonneg_of_minimum`, and the same script on `Adverse.lean` requiring `FSCChecks.WP15.negative_signed_row`, `FSCChecks.WP15.saturated_low_row_strict`, and `FSCChecks.WP15.antipodal_zero_kappa`. All exited 0; production audit counts are 25 and 14, adverse count is 5. Next critical consumers are the lead's PD exclusion, the analyst's actual support/energy scalar slope assembly, and positive-contact equality.
