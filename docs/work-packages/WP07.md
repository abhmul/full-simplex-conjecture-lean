# WP07: Threshold Hessian and Peano expansion

- Status: implemented, warning-clean and independently source-reviewed; lead serial integration pending
- Owner: worker analytic_preflight; integration/Git owner remains the lead
- Dependencies: WP04, WP05
- Owned files: `FSC/Gaussian/ThresholdExpansion.lean`, announced projected-normal/conditional-law helper files, this card and checks/wp07-threshold; the shared PeanoTaylor module remains owned by its original worker
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/SUPPORT_NOISE_VARIATION.md sections 2-3; MATHEMATICAL_REVIEW.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Apply support C1 twice near a positive common threshold for distinct scores; prove the exact Hessian and vector-domain second-order expansion.

## Acceptance contract

Threshold Peano2 with exactly the canonical q; no covariance Hessian or unequal-threshold generalization.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Ownership assigned by the lead before proof edits on 2026-09-08. The worker read SUPPORT_NOISE_VARIATION sections 2-3 and the exact internal distinctness/positive-threshold scope. The actual support C1 producer and generic PeanoTaylor theorem are compiled; the task is to supply original and conditional support neighborhoods, identify the canonical q Hessian and feed the genuine expansion into the already compiled explicit-Peano normalized derivative. Ordinary Gram realization is used here without singular rank padding. New helper-file ownership will be announced to the lead and coordinated with the support worker before edits.

## Actual universal threshold regularity milestone

The lead approved exclusive FSC/Gaussian/ProjectedNormals.lean, ConditionalCaps.lean and ThresholdExpansion.lean before edits. The retained index is the transparent subtype RetainedPair G i = {j : Fin n // j ≠ i ∧ -1 < G i j ∧ G i j < 1}. projectedNormal uses the ordinary gramVectors realization and the frozen pairSigma; conditionalSupport G i b j is (b_j-G_ij*b_i)/pairSigma. At the common threshold it equals the frozen pairSupport exactly. This convention is shared with the facet worker; cross-dimensional facet compatibility will transport cap masses by equality of Gram matrices rather than changing threshold geometry.

ProjectedNormals proves unit projected normals, orthogonality/reconstruction, positive actual supports, and both original and retained NoCoincident conditions. The scalar support_ratio_injective theorem proves the key separation implication using the exact squared normalization identity; it does not require projected normals to be distinct at arbitrary supports. ConditionalCaps proves thresholdCDF_eq_capMass_gram under the actual Gaussian score law, the actual original-slice-to-retained-cap event identity on a neighborhood where antipodal constraints are automatic, and both C1 and the exact derivative of the sliceMass via the proved supportGradient. map_gramSlice_singleLaw and sliceMass_gram_eq_singlePinMass identify the frozen one-pin law for every real pin without distinctness or positivity assumptions.

ThresholdExpansion proves hasFDerivAt_thresholdCDF under the actual original NoCoincident condition, an eventual derivative field near each common t > 0 for distinct correlations, ContDiffAt ℝ 1 of that field, contDiffAt_two_thresholdCDF, and peano2_thresholdCDF on the full Coord n domain. The Peano derivative fields are thresholdGradient and its actual Frechet derivative. The ordinary scalar endpoint hasDerivAt_cdf states exactly HasDerivAt (cdf G) (boundarySlope G t) t under correlation, distinctness and t > 0. There is no rank or dimension assumption in these regularity endpoints. Canonical q identification of the second derivative remains in progress; this milestone does not close the universal half-trace derivative.

Commands: lake build FSC.Gaussian.ProjectedNormals --wfail exited 0 (3245 jobs, target 1.7 seconds); ConditionalCaps exited 0 (3247 jobs, target 1.8 seconds); ThresholdExpansion exited 0 (3252 jobs, target 1.9 seconds). The final warning-error compiler invocation on ThresholdExpansion exited 0. Fresh specialized-Python audit of FSCProbes/ThresholdExpansionAudit.lean required projected NoCoincident, actual cap bridge, explicit slice derivative, canonical singlePinMass bridge, hasDerivAt_cdf, universal C2 and vector Peano; it exited 0 for 16 declarations. checks/wp07-threshold/threshold-expansion-axioms.jsonl preserves exact endpoint types and transitive reports using only propext, Classical.choice and Quot.sound. Independent lead review is requested.

Compiler evidence: an opaque def alias for the retained subtype prevented its canonical coercion to Fin n; changing it to an abbrev preserves the exact subtype and allows standard coercions/finite instances. Broad simplification can rewrite real inner self to norms before the Gram identities; explicit inner-product expansion avoids that loop. The normalResidual API is a CLM, so its public normalResidual_apply lemma is used rather than unfolding the CLM structure. Continuous linear maps need explicit real scalar/index parameters for the retained support map; coercion-level function equalities use a named conditionalSupportMap_coe. No covariance differentiability, full rank, reference tie exclusion or other mathematical hypothesis was introduced.

## Closed canonical Hessian and universal derivative

The later milestone closes the analytic inputs left open above. ConditionalCaps now proves the exact sequential gramPairSlice affine expression, map_gramPairSlice_pairLaw, the actual projectedSlice_event excluding both pinned coordinates, sliceMass_projected_eq_pairSuccess and q_eq_projected_slice. ThresholdExpansion proves the canonical row-coefficient derivative and thresholdHessian_apply: the actual Frechet derivative of thresholdGradient evaluated on coordinate basis vectors is stress G t i j minus the diagonal t-times-first-derivative term. The final FSC.hasDerivWithinAt_normalizedAdd takes only correlation G, DistinctScores G, PSD L and t > 0, and gives the actual right derivative on Ici 0 equal to (1/2)*trace(stress G t * L). It has no Peano, Hessian, density, rank, support, tie or analytic-oracle premise. It actually proves the derivative in every dimension; the downstream minimizer statements retain their needed n >= 3 scope.

On base revision 82ee3eced798c7b36e4f174b9e6fb3b9d09f5cb9 with the owned additions, lake build FSC.Gaussian.ThresholdExpansion --wfail exited 0 (3257 jobs, target 2.6 seconds). The warning-error compiler also exited 0. The fresh importing audit of FSCProbes/ThresholdExpansionAudit.lean passed 28 declarations and explicitly required the actual-law, q, Hessian, Peano and universal derivative endpoints; checks/wp07-threshold/threshold-expansion-axioms.jsonl preserves the printed signatures and transitive reports with only propext, Classical.choice and Quot.sound. The lead read the final source in full and independently accepted the event equivalence, canonical q normalization, Hessian orientation and diagonal cancellation.

The separate UniversalAnalyticAdverses consumer imports the installed production theorem. Its triangle_universal_derivative matches exactly the independently proved -(2/5)*q_12 coefficient for the rank-increasing triangle direction. The five rational planar normals give a genuine rank-at-most-two, distinct 5-by-5 correlation Gaussian law, actual vector Peano expansion and normalized identity-noise variation, despite antipodes and retained redundant directions. Negative controls prove that duplicate scores fail DistinctScores, the duplicate tied-support cap is nondifferentiable, and the actual antipodal two-site CDF is nondifferentiable at zero. Thus the internal exclusions have real counterexamples and remain absent from the eventual public comparison contract. lake build FSCProbes.UniversalAnalyticAdverses --wfail exited 0 (3791 jobs, target 2.5 seconds); its fresh 11-declaration importing audit and required endpoints are stored in checks/wp07-threshold/universal-adverses-axioms.jsonl.

Additional compiler evidence: expanding the explicit triangle Gram before using q symmetry changes the syntactic q arguments; rewriting the already-proved q equalities under the same explicit matrix repaired the regression consumer. Evaluation of the Hessian differentiates a fixed CLM evaluation and reverses the two coordinate roles; the canonical stress symmetry resolves that orientation explicitly. No mathematical hypothesis or trust boundary was altered.
