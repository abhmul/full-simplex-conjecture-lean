# WP06: Support quadratic tangent from Prekopa

- Status: integrated; independently reviewed and freshly audited
- Owner: root; sole owner of QuadraticTangent.lean, this card and checks/wp06 before edits
- Dependencies: WP01, WP05, WP11
- Owned files: `FSC/Support/QuadraticTangent.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md Actual support caps; pro-return/INTERFACES.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Use existing Prekopa for support log-concavity and reciprocal convexity; consume an explicit proved derivative at the actual point.

## Acceptance contract

Quadratic tangent under explicit actual-point derivative; no differentiability condition at the reference. Later instantiate with WP05.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Root started after completing WP11 analysis and padding. Read teaching manuscript 6.1 and the Gaussian support section, reviewed DOSSIER actual support caps/reciprocal comparison and INTERFACES tangent contract. The source manuscript proves Prekopa for teaching; implementation consumes the pinned public WSC theorem. First exact consumer: Gaussian density times the indicator of the joint convex halfspace set, marginalized over Pi coordinates, equals the actual cap law. Then derive reciprocal convexity and its tangent at the actual support under an explicit actual-point derivative; no differentiability or no-coincidence premise at the reference. The exact final interface remains the quadratic support inequality.

Accepted implementation: `isLogConcave_capMeasure` identifies the actual Gaussian cap with the marginal of the joint convex indicator times its normalized density and applies public pinned `WeakSimplex.isLogConcave_lintegral_right`. `capMass_logConcave`, `capMass_pos`, and `convexOn_inv_capMass` derive real-valued log-concavity, positivity on positive supports, and reciprocal convexity. No new Prekopa proof or private-name import was introduced. `quadratic_support_tangent_of_hasFDerivAt` proves `l (b-c) ≤ capMass w b ^ 2 / capMass w c - capMass w b` under an explicit actual derivative. The closed `quadratic_support_tangent` discharges this derivative using WP05 for unit normals and `NoCoincident w b`. Both supports must be positive; the reference c needs no no-coincidence or differentiability condition.

Commands at base revision 4089807: `lake build FSC.Support.QuadraticTangent --wfail` exited 0; fresh required-endpoint `scripts/audit_axioms.py checks/wp06/Audit.lean --required FSC.quadratic_support_tangent --required FSC.isLogConcave_capMeasure --save-json checks/wp06/audit.jsonl` passed 10 declarations; the same fresh checker on `checks/wp06/Adverse.lean` passed 3. All transitive closures are subsets of propext/Classical.choice/Quot.sound. Expanded statements are saved in the JSON evidence. Adverses cover identical redundant normals at different actual supports with a tied, nondifferentiable reference, and the empty index/zero-dimensional cap. The tied-reference counterexample remains explicit.

Independent source review by wp00_algebra accepted the actual density normalization, convex joint cap, public Prekopa application, finite-mass conversion, positivity including empty/zero-dimensional cases, reciprocal convexity and tangent sign. In particular the inverse derivative is `l(b-c)/h(b)^2`; clearing its positive denominator gives precisely the stated inequality. Review confirmed that no reference derivative was smuggled in.

Preserved implementation failures: a guessed qualified withDensity API did not exist; actual measure APIs were used. An under-typed measurable section caused a deterministic 200000-heartbeat definitional-equality timeout. Explicitly typing the product section and simplifying composition solved it without raising the limit. Indicator equalities required named actual membership proofs. None changed the mathematical interface. The next consumer is the padded facet comparison in WP19.
