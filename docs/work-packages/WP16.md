# WP16: Facet compatibility and padded component identities

- Status: integrated; independently reviewed and freshly audited
- Owner: `/root/wp00_imports`, assigned by lead on 2026-09-08 before edits
- Dependencies: WP04, WP05, WP11, WP12
- Owned files: `FSC/Facet/Compatibility.lean`, `FSC/Facet/PaddedNormals.lean`, `FSC/Gaussian/SubspaceGaussian.lean`, `FSC/Support/GramInvariance.lean`, `checks/wp16/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md equations P,F,E; sources/TEACHING_MANUSCRIPT.md chapters 5-7
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Identify pin mass, actual support derivatives and energy using the same q and realization.

## Acceptance contract

All normalization equalities together, positive u/e and H=u+e in dimension n-2.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned exclusive ownership of `FSC/Facet/Compatibility.lean`, this card and `checks/wp16/`. Read dossier equations P,F,E and actual manuscript §§5.3–5.5 and chapter 7. Ordinary Gram/projected-normal threshold work remains owned by `analytic_preflight`; this package uses the fixed dimension `n-2` only for physical cap/energy identities. First concrete missing consumer is the equality between an ambient unit-normal residual pushforward and the subtype-embedded standard Gaussian on its orthogonal complement. The WP12 isometry and standard-law transport are already proved, but do not assert that residual bridge.

The worker announced `FSC/Gaussian/SubspaceGaussian.lean` to the lead before edits. It proves the missing actual projection and embedded residual Gaussian laws, then the exact affine image of the frozen `sliceLaw` from the padded tangent coordinates. No conditional distribution version or project-specific certificate is introduced.

The worker subsequently announced `FSC/Support/GramInvariance.lean` and `FSC/Facet/PaddedNormals.lean` before their edits. The intended sharing interface is exact equality of cap-mass functions for any two finite normal families with equal Gram matrices, even at tied supports. The padded projected tangent normals will have the same Gram as the ordinary projected normals used by WP07. Thus canonical one-/two-pin normalization can be transported between those actual laws without a second independent pair-regression derivation. The analyst's existing retained-index, support and q conventions remain frozen.

## Accepted physical representation helpers

`FSC/Gaussian/SubspaceGaussian.lean` now proves the actual orthogonal-projection pushforward to the standard Gaussian on a submodule, the ambient residual as the subtype embedding of that law, and the exact affine pushforward from `Coord (n-2)` to the frozen `sliceLaw` at every real pin. The lead installed with `--wfail`, independently reviewed all source, and accepted the covariance-based mean/covariance proof. Fresh `checks/wp16/SubspaceAudit.lean` audit passed for five endpoints; output is `checks/wp16/subspace-axioms.jsonl`.

`FSC/Support/GramInvariance.lean` proves actual all-support cap-mass equality from equal Gram matrices, unit-normal `NoCoincident` transport, and equality of the two actual support gradients by uniqueness of genuine Frechet derivatives. No derivative is requested at the equal-support reference. The final source was rebuilt with `--wfail` after an additive edit; the lead independently reviewed the final version. Fresh six-endpoint importing audit passed in `checks/wp16/GramAudit.lean`, output `checks/wp16/gram-axioms.jsonl`.

`FSC/Facet/PaddedNormals.lean` constructs projected unit normals in the full tangent dimension `n-2`, proves their Gram matrix equals that of the ordinary WP07 projected normals, and transports actual cap-mass functions and actual support gradients. The lead installed warning-clean and independently reviewed all 104 source lines. Fresh seven-endpoint importing audit passed in `checks/wp16/PaddedAudit.lean`, output `checks/wp16/padded-axioms.jsonl`. All three fresh audits report only `propext`, `Classical.choice`, `Quot.sound`.

Commands used for each source were `lake env lean -DwarningAsError=true PATH`, followed by the lead's serial `lake build MODULE --wfail`, then `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py AUDIT --required ENDPOINT ... --save-json OUTPUT`. The three importing audit files list the exact required endpoints. Current pins remain WSC `a204c53cae45652d12524132dbb9a2e0ffe8cf78`, mathlib `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`, Lean 4.31.0.

## Compiler evidence and refinements

The exact public Gaussian covariance route required extensionality (`ext x y`) before `covarianceBilin_map`; the final submodule-inner identity is definitional after the adjoint projection identity. The reindex equivalence's `surjective.forall` theorem had the reverse orientation from the guessed rewriting direction; the compiled proof explicitly uses its symmetry. Real inner-product equality characterizations required explicit `(𝕜 := ℝ)` to avoid stuck scalar instances. No private declaration was imported or adapted for these helpers.

The first `Compatibility.lean` retained-sum attempt tried rewriting a sum after `simp_rw [mul_comm ...]` and failed at semireducible matrix-function elaboration. The accepted repair applies the already compiled WP07 `sum_retained_q` to an explicit `Fin n → ℝ` function and uses `Finset.sum_erase_eq_sub` with `q_self`. An unannotated energy-sum index was inferred as `Fin n` before the later `sliceMass` argument required `RetainedPair`; the actual proof explicitly annotates the retained subtype. Likewise the support-direction subtraction requires both function domains annotated. These are elaboration repairs, with unchanged mathematical architecture.

## Compatibility acceptance receipt

`FSC/Facet/Compatibility.lean` is stable, installed with `--wfail`, and independently source-reviewed by the lead. Its 18 theorem endpoints passed the fresh importing audit `checks/wp16/Audit.lean`, saved to `checks/wp16/axioms.jsonl`. An earlier progress message counted 19; the actual exact endpoint count is 18, with `facetPressure` and `facetEnergy` separately transparent definitions. All fresh audited closures contain only `propext`, `Classical.choice`, `Quot.sound`.

The accepted statements identify each retained derivative coefficient with `sigma*q/phi(t)`, the actual support-direction derivative with `t*rowK/phi(t)`, and the all-one direction with `rowC/phi(t)`. `capEnergy_padded_eq` derives the full physical `n-2` second moment from actual dilation and actual support differentiation. `singlePinMass_eq_pressure_add_energy` and the two strict-positivity theorems use this same cap. `singlePinMass_sub_pressure_pos` removes the realization parameter by constructing an actual padded realization from a singular correlation and proving positivity of its actual Gaussian integral. No analytic certificate or unproved regularity input remains. `normalized_facetPressure` matches the pressure ratios to `rowK/kappa`; `cast_facetDimension` explicitly identifies the natural dimension cast with real `n-2`. `supportGradient_padded_reference` evaluates the direction `s-b` for every real reference `s` and never differentiates at that reference.

The documented square and five-direction consumers are in `checks/wp16/Adverse.lean`. Its fresh five-endpoint audit passed, saved to `checks/wp16/adverse-axioms.jsonl`. The square input is the actual rank-two Gram of `e1,e2,-e1,-e2`; its intrinsic retained cap is proved exactly to be `[-t,t]`. For every actual selected padded facet, `square_actual_padding` proves its second moment equals the intrinsic interval second moment plus one whole cap mass, using all-support Gram invariance, derivative uniqueness for dilation, and the exact product padding identity. This connects the actual canonical facet to the numeric `2H-2t*phi(t)` strip calculation already audited in WP11. The n=5 input is the exact planar five-direction Gram; strict energy positivity is proved for every row, and the component theorem explicitly uses an actual integral in dimension three divided by three, despite original rank two.

Adverse compiler repairs are preserved here: broad simplification of `inner x x` converted concrete Gram diagonals to norm goals before coordinate expansion; expanding only `Matrix.gram_apply` and `PiLp.inner_apply` first resolved this. `norm_num at hj' ⊢` emitted `No goals to be solved` when a contradictory retained-index property had already closed the goal; the successful command uses `at *`. No heartbeat limit or trust expansion was used. Final `git diff --check` for all owned production/check/card files returned exit 0.

At the user's travel pause, all owned WP16 source is stable and all compiler/audit processes are finished. Production source has the lead's independent review; the fresh concrete adverse receipt is ready for the lead's final integration review. No worker commit was made. The next assigned package WP24 has its separate checkpoint and does not affect this receipt.
