# WP13: All-correlation CDF continuity and compactness

- Status: reviewed
- Owner: `/root/wp00_algebra`, assigned by integration lead before edits; root retains Git/integration
- Dependencies: WP02
- Owned files: `FSC/Gaussian/CDFContinuity.lean`, `FSC/Compactness.lean`, this card and `checks/wp13/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/SOURCE_MAP.md Levy/Portmanteau; pro-return/DOSSIER.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Use characteristic functions, sequential Levy and null-frontier Portmanteau; prove finite-dimensional continuity and compact elliptope.

## Acceptance contract

Joint continuity for EVERY correlation and threshold, including duplicates; compact shape minimizers; real/measure conversions.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned this independent analytic branch after WP04 acceptance. Ownership is recorded before edits. The fixed contract includes all PSD correlations, duplicates, every real threshold and joint shape/threshold continuity. The worker reads the reviewed interface, exact public Levy/Portmanteau source and pinned callers before implementation. No support-Hessian, distinctness or full-rank dependency is introduced.

## First complete compiler evidence

`lake env lean -DwarningAsError=true FSC/Gaussian/CDFContinuity.lean` exited 0. The exact named endpoint `continuous_cdf_on_correlations` has the frozen type for the correlation subtype times all of ℝ. The stronger vector-threshold endpoint `continuous_thresholdCDF_on_correlations` also compiles. `tendsto_multivariateGaussian` uses public `ProbabilityMeasure.tendsto_of_tendsto_charFun` for arbitrary convergent means and PSD covariances, including rank changes. The actual threshold event is mapped to the fixed zero orthant with mean −b before applying the ENNReal Portmanteau theorem and finite-measure toReal continuity. `gaussian_frontier_thresholdEvent_null` assumes only PSD and positive marginal variances, not full rank or distinctness. No theorem assumes an arbitrary conditional distribution or analytic certificate.

The frontier proof is an original closed-orthant/strict-interior argument. The scalar coordinate-null step uses public `measurePreserving_eval_multivariateGaussian` and `noAtoms_gaussianReal`. Pinned WSC `Orthant/Singular.lean` private null-frontier steps and `Product/PositiveLowerBound.lean` private Portmanteau wrapper were inspected as API examples, but no private declaration is called; the stronger arbitrary-mean/vector-threshold argument is proved locally. The source meanings and positive-marginal caveat in `INTERFACES.md` are retained.

`lake env lean -DwarningAsError=true FSC/Compactness.lean` exited 0 for the closed PSD cone, closed correlation set, compact entrywise [−1,1] box containment and identity-matrix nonemptiness. The ordinary Gram entry bound from WP04 is reused. This adds an implementation dependency on WP04's proved convenience helpers; no rank or derivative dependency is added. Actual shape-minimizer existence will be compiled after the stable CDF target is installed.

Failed API/elaboration evidence: subtype/Matrix synonym first-countability did not synthesize through the default search path. `Mathlib.Topology.Bases` and explicit local applications of the existing finite-Pi first-countability and `TopologicalSpace.Subtype.firstCountableTopology` provide the exact inherited topology. `checks/wp13/TopologyProbe.lean` records those probes. A monolithic product-space characteristic-function continuity composition timed out even at 800000 heartbeats. Actual LSP goals and temporary trace output located the issue at composition elaboration after the continuity fact had already been proved. Splitting the limit into its scalar inner-product and finite quadratic-form terms compiled at the default 200000 heartbeats; no heartbeat increase remains. `WeakSimplex.Coord.ofFun` needed its transparent definition unfolded for continuity automation. These are implementation corrections without a mathematical architecture change.

The stable CDF target installed warning-clean. The lead independently reviewed the complete CDF source and accepted the all-PSD characteristic-function/Levy argument, positive-marginal frontier nullity, mean-shifted fixed orthant and sequential upgrade. The original proposed `cdf_pos` signature is retained with its redundant dimension/correlation binders explicitly underscored; the proof establishes its conclusion on a wider domain because every total centered Gaussian gives positive mass to an open neighborhood of zero.

`FSC/Compactness.lean` then compiled warning-clean with `compactSpace_correlations`, `continuousOn_cdf_correlations` and `exists_cdf_minimizer n t`, for every natural n and every real t. The minimizer is an actual matrix satisfying `IsCorrelation`, with its value no larger than that of any correlation. The continuity restriction proof required explicitly changing the opaque `CorrSet` restriction to its transparent correlation subtype before composition; leaving that conversion to elaboration timed out. This is a representation correction, not an extra compactness or continuity hypothesis.

## Adverse and local evidence

`/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp13/Adverse.lean --required FSCChecks.cdf_duplicate_limit --required FSCChecks.deterministic_frontier_mass --save-json checks/wp13/adverse-audit.jsonl` exited 0 for four named endpoints, all with exactly `propext`, `Classical.choice`, `Quot.sound`. The duplicate covariance `(1-s)J+sI`, at s=2^(-k), converges in the actual CDF for every real threshold to the singular duplicated-score law. The zero-covariance one-dimensional law puts mass one on the zero-threshold orthant frontier, explicitly falsifying omission of the positive-marginal premise. The topology API probe also compiles warning-clean.

Stable source SHA-256: `CDFContinuity.lean` = `00b09d108e037b5ccfc8c8060a329dd45711c19f6bebabc288dce5101380cd1c`; `Compactness.lean` = `0181e2de5b9b3ea50d58644812e5ed11a844aac6c963575b88bd828a96b3aeed`. `git diff --check` on this worker's WP13 and additive WP04 scope exited 0.

## Accepted package receipt

The lead independently reviewed the complete final Compactness source and accepted its closedness, compactness and actual minimizer proof. Both stable production targets installed with warnings as errors. `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp13/Audit.lean --required FSC.tendsto_multivariateGaussian --required FSC.gaussian_frontier_thresholdEvent_null --required FSC.continuous_thresholdCDF_on_correlations --required FSC.continuous_cdf_on_correlations --required FSC.isCompact_corrSet --required FSC.exists_cdf_minimizer --save-json checks/wp13/audit.jsonl` exited 0, passing all 17 endpoints. Every report contains exactly `propext`, `Classical.choice`, `Quot.sound`; expanded required statements are in the fresh JSON. These endpoints have no still-conditional analytic hypothesis. Public FSC comparison/equality and clean-checkout release acceptance remain separate unfinished obligations.
