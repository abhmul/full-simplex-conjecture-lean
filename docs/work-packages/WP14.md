# WP14: Componentwise scalar comparison and equality

- Status: reviewed; integration owned by the lead
- Owner: worker `/root/wp00_algebra`; integration/Git owner `/root`
- Dependencies: WP01
- Owned files: `FSC/Scalar/Componentwise.lean`, this card, `checks/wp14/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: sources/COMPONENTWISE_COMPARISON.md section 1; pro-return/DOSSIER.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove full finite scalar conversion and exact equality from positive u,e,weights; preserve zero-component counterexample.

## Acceptance contract

Comparison and equality theorems, not just rational remainder identity; all component positivity assumptions explicit.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Ownership assigned by the lead before edits on 2026-09-08. The worker read `COMPONENTWISE_COMPARISON.md` section 1 and the explicit scalar signatures in `INTERFACES.md`. The exact contract is `componentwise_compare` and `componentwise_equality` with finite nonempty index type, positive u/e/omega and M, normalized omega, and the original componentwise quadratic inequalities. All divisions are real; the equality conclusion is the three simultaneous component identities in the dossier. Scalar implementation is independent of the shared Gaussian definitions. No shared root, pin or Git mutation is assigned to this worker.

The algebraic rational remainder in `FSCProbes/Algebra.lean` is available as prior compiler evidence but production will use a local, source-provenanced helper rather than import probe infrastructure. Lean LSP is absent from the available tool surface; actual compiler diagnostics will drive implementation. The lead currently owns broad Lake target installation; the worker uses individual compiler invocations and its own local audit.

## Kernel-checked local endpoints

`FSC/Scalar/Componentwise.lean` proves `FSC.componentwise_compare` and `FSC.componentwise_equality` with the reviewed mathematical signatures. The equality theorem retains the positive-weight premise under binder name `_ho`, since that premise is not needed in this direction of the proof. This is not a weakened conclusion or an omitted public assumption. No Gaussian definitions or project-specific analytic input is imported.

The comparison proof supposes the total mass is below M, applies the exact two-variable quadratic inequality at each component, cancels the strictly positive e_i, and contradicts the equal normalized sums. The equality proof first proves omega_i ≤ e_i/E, uses equality of finite sums to get equality componentwise, then sets the exact quadratic loss to zero and obtains u_i/U = e_i/E and x_i = M omega_i. Reusable public helpers are `FSC.quadratic_remainder` and `FSC.quadratic_split_le`.

`lake env lean -DwarningAsError=true FSC/Scalar/Componentwise.lean` exited 0 with empty output after both endpoints were present. `git diff --check` exited 0. The worker requested serial target installation from the lead before compiling the importing audit. No worker commit or shared-root edit occurred.

## Adverse case

`checks/wp14/Adverse.lean` proves `FSCChecks.emptyComponentCounterexample` for u=e=(1/2,0), omega=(1/2,1/2), M=6/5. Its statement includes nonnegative components, positive weights and M, weight normalization, both componentwise inequalities, total mass strictly below M, and the explicit zero second component. `lake env lean -DwarningAsError=true checks/wp14/Adverse.lean` exited 0. Fresh compiler output reports exactly `[propext, Classical.choice, Quot.sound]` for this theorem. Thus replacing the positive occupied-component contract by nonnegativity fails; the negative test is not a Gaussian example.

## Compiler/API corrections retained

The first comparison proof reached the final normalized-sum step but failed at `rw [homega, ← Finset.sum_div]` because the narrow imports did not expose `Finset.sum_div`; the remaining goal was `False` under `hsum : 1 < ∑ i, e i / E`. Exact local source search located the declaration in `Mathlib/Algebra/BigOperators/Field.lean`. Adding that narrow import resolved the failure.

Warnings-as-errors then rejected unused comparison binders ho and hM. The proof now uses their actual positivity to infer the positive squared numerator from the componentwise inequality before dividing; no linter is disabled. The equality proof names its mathematically redundant retained weight-positivity premise `_ho`.

The adverse file's guessed import `Mathlib.Data.Matrix.Notation` failed with a missing object file. Local source search found the correct path `Mathlib.LinearAlgebra.Matrix.Notation`; using that import compiled. No dependency pin, mathematical contract or trust policy changed in these corrections.

## Fresh audit and independent review

The lead installed `FSC.Scalar.Componentwise` with exit 0 before the importing consumer. After reading the required agent-Python README, the worker ran `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp14/Audit.lean --required FSC.quadratic_remainder --required FSC.quadratic_split_le --required FSC.componentwise_compare --required FSC.componentwise_equality --save-json checks/wp14/audit.jsonl`. Exit 0: `Fresh axiom audit passed for 4 declarations.` The fresh JSON file contains the ordinary and fully expanded endpoint types and four matched transitive axiom reports. Each endpoint uses exactly `[propext, Classical.choice, Quot.sound]`.

The lead independently reviewed the source and reported that the exact signatures are preserved and the equality reasoning is sound. No conditional project-specific result remains in WP14. The Gaussian minimizer-slope consumer WP19 still owes the componentwise inequalities and positive geometric masses; this scalar theorem does not supply those analytic inputs.

Accepted source SHA-256: `c4ff85d3922ff71a120d960603b631720a06facad10bfafcaedbd897035dbd30` for `FSC/Scalar/Componentwise.lean`; audit source SHA-256 `134e0b229436252060cb6b1f34f4696c09bf0694290b393f12090ec6ddeb4d4e`; adverse source SHA-256 `12ef1b8b2c42134ddd8650397ab50adb89cde7901be9ffb0c862dd6c636aa42f`. Environment pins match WP00. The worker made no commit; the lead records the coherent integration revision.
