# WP03: Canonical one-pin law

Final-suite evidence correction: after the WP25 run exposed unqualified #print/report-name mismatch in the analogous WP02 adverse file, root qualified the four commands in checks/wp03/Adverse.lean before the fresh full-suite rerun. Mathematical proof bodies and the fail-closed audit parser were unchanged. Current and clean reconstruction receipts are retained under checks/release.

- Status: integrated; independently reviewed by the lead
- Owner: worker `/root/wp00_algebra`; integration/Git owner `/root`
- Dependencies: WP02
- Owned files: `FSC/Gaussian/SinglePin.lean`, this card, `checks/wp03/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/SUPPORT_NOISE_VARIATION.md section 2
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Construct residual/pin joint Gaussian law, independence and Fubini on original indices, without conditioning-version ambiguity.

## Acceptance contract

Single-pin factorization and integration identity for measurable remaining-coordinate events; singular residual allowed.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned WP03 as the next task after WP02 audits on 2026-09-08; ownership is recorded before edits. The worker read the card and `SUPPORT_NOISE_VARIATION.md` section 2, and will use frozen `singleMean`, `singleCov` and `singleLaw` from `FSC/Gaussian/PinDefinitions.lean`. The source mean convention uses columns G k i; correlation symmetry relates it to the manuscript's row convention. The acceptance target is the actual residual/pin product law and a measurable Fubini formula with arbitrary fixed real pins and all PSD ranks. No arbitrary conditional-probability version is introduced. Shared definitions, pins, roots and Git remain owned by the lead.

## Kernel-checked canonical factorization and Fubini

The explicit matrix `singleResidual G i = 1 - vecMulVec (fun k ↦ G k i) (Pi.single i 1)` acts by x_k − G_ki x_i. `singleResidual_covariance` identifies its PSD congruence covariance with the frozen `singleCov`; `singleCov_posSemidef` and `singleResidual_pinned` include singular and deterministic residuals. `map_singleResidual` is a consumer of the actual affine-image law. `map_singleMean_add` identifies every fixed real translate with `singleLaw G i t`.

`singleResidual_indep_pin` constructs a joint `HasGaussianLaw` using the product CLM of the residual and the coordinate projection, then proves independence from zero coordinate cross-covariances through the public Hilbert-valued covariance independence theorem. It does not infer joint Gaussianity from marginals. `map_singleResidual_pin` gives the exact residual Gaussian × standard real Gaussian product law.

`lintegral_singlePin` states, for every measurable f : Coord n × ℝ → ℝ≥0∞, that integrating f(x,x_i) against N(0,G) equals the iterated integral of f(y,t) against the explicit `singleLaw G i t` and N(0,1). Its proof uses the actual product law, exact reconstruction, and Tonelli in the needed order. `measure_singlePin` specializes to an arbitrary measurable event. The sole covariance hypothesis is the transparent `WeakSimplex.IsCorrelation G`; no distinctness, positive threshold or rank condition occurs.

`lake env lean -DwarningAsError=true FSC/Gaussian/SinglePin.lean` exited 0 after all the factorization/Fubini endpoints above were present. The lead was notified so the support branch could consume the target before local adverse checks were complete. A subsequent source addition proves `singleLaw_pin_ae`, the deterministic pinned coordinate under every fixed-t law.

## Compiler/LSP experiments retained

The residual matrix-vector simplification initially unfolded general `lin_apply` before its specialized residual rule, leaving `(singleResidual G i *ᵥ x.ofLp) k` in goals that were intended to be scalar algebra. Targeted `rw [lin_singleResidual_apply]` repaired the application boundary. Covariance expansion similarly needed an explicit formula for only the transposed residual factor, preserving the named `singleResidual_mul_apply` consumer on the other factor. The remaining `Matrix.row` and `Matrix.col` wrappers were exposed explicitly instead of introducing new instances or transparency options.

An LSP call to `scripts/lean_lsp.py FSC/Gaussian/SinglePin.lean --line 113 --column 3` reported that inferring `MemLp` directly from `IsGaussian.memLp_two_id.continuousLinearMap_comp (EuclideanSpace.proj j)` left the measure and scalar projection underdetermined. The proof now uses the public `IsGaussian.memLp_dual` with the exact source measure and exponent supplied. A composed residual projection inferred correctly. No placeholder or Gaussianity assumption was added.

The last Tonelli step initially tried rewriting `lintegral_map` with an inferred composition `f ∘ (fun a ↦ (id a, ?t))`, which did not syntactically match `fun y ↦ f (y,t)`. Giving the measurable section its explicit function type and applying the map integral equality in reverse resolved it. The event wrapper uses `(1 : Coord n → ℝ≥0∞)` rather than a syntactically different constant lambda to match `lintegral_indicator_one` under the narrow simplifier.

## Fresh audit rejected a stale installed target

The first fresh importing audit exited 1: `FSC.singleLaw_pin_ae` was unknown at audit lines 6 and 23, while the direct source compiler had checked that declaration successfully. The lead's target build had overlapped the final source addition before the worker received a requested mutation hold; the installed artifact contained the earlier 14 declarations. Failed compiler JSON is preserved as `checks/wp03/audit-stale-target.jsonl`. The worker stopped source mutations and requested a forced rebuild before any acceptance claim. This is why direct source compilation, installed consumers and fresh endpoint coverage are separate gates.

The first duplicate-score adverse check also failed because `Matrix.vecMulVec` retained a `Matrix.of` wrapper under a broad simplification. Its rejected proof generated a diagnostic `sorryAx` report for the failed check, so that run is not acceptance. The repair supplies an explicit entrywise matrix equality before applying public `Matrix.posSemidef_vecMulVec_self_star`; it does not use a placeholder. The antipodal and deterministic-law adverse declarations checked in that same run with only allowed axioms.

## Fresh accepted audit and adverse cases

After the lead rebuilt the stable source target and checked the imported pin-support symbol, the worker reran `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp03/Audit.lean --required FSC.map_singleResidual_pin --required FSC.singleLaw_pin_ae --required FSC.lintegral_singlePin --required FSC.measure_singlePin --save-json checks/wp03/audit.jsonl`. Exit 0: `Fresh axiom audit passed for 15 declarations.` The fresh output contains the printed/expanded key statements and all matched transitive axiom reports, each using exactly `[propext, Classical.choice, Quot.sound]`. The rejected stale-target output remains separately preserved.

`lake env lean -DwarningAsError=true checks/wp03/Adverse.lean` exited 0 after the explicit matrix-equality repair. It checks that the duplicate matrix and antipodal matrix are actual correlations and that their one-pin laws are exactly Dirac at (t,t) and (t,−t), respectively, for every real t. All four fresh adverse axiom reports contain exactly the allowed three axioms. The production theorem itself retains all distinct and duplicate correlations.

The lead independently reviewed the residual covariance, joint Gaussianity before independence, product law, fixed-t law/support and arbitrary measurable-test Fubini. The review confirmed no leaked distinctness, positive-threshold or rank hypothesis. Source SHA-256 is `21d7971ab4ca2aa4cf8c6edb01bb551a32716e2483e2f37554ad4ec9ca729b04`; pins remain WP00's. `git diff --check` exited 0. No worker commit occurred. The universal pair regression WP04 and generic support differentiation WP05 are the next consumers; their analytic obligations are not discharged by this one-pin law.
