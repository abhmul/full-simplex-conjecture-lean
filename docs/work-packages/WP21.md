# WP21: Two-site base and nonpositive thresholds

- Status: integrated; independently reviewed and freshly audited
- Owner: `/root/wp00_algebra`, assigned by lead before edits; root retains Git/integration
- Dependencies: WP02
- Owned files: `FSC/Simplex/Definitions.lean`, `FSC/Simplex/BaseCases.lean`, this card and `checks/wp21/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md final base-case paragraphs
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Use inclusion-exclusion and Gram-support positivity for n=2; simplex zero sum and nonatomic marginals for t<=0.

## Acceptance contract

All-correlation base comparison; strictness at t>0 except antipodal; D_n(t)=0 for t<=0.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned this package after WP13 source acceptance, with the final fresh WP13 audit running. The worker has read the card, `DOSSIER.md` base-case paragraphs and `TEACHING_MANUSCRIPT.md` Chapter 1 lines 19–216. The operative n=2 strictness proof uses ordinary-Gram open-support positivity as reviewed in the dossier, so it handles the duplicated covariance directly without imposing full rank. Nonpositive thresholds use the actual simplex law's zero sum and a nonatomic coordinate. All dimensions n≥2 and all real thresholds remain in the final base comparison.

The lead approved a small separately installed `FSC/Simplex/Definitions.lean` containing the public simplex correlation foundation, so WP17 may consume it before the base-case proof is complete. The pinned WSC public `gramNormalization` theorem exposes `regularSimplexGram` correlation as a conjunct, although the direct declaration of that property is private. The proof will use that public theorem applied to the identity correlation, not the private generated name.

## Stable foundation

`FSC/Simplex/Definitions.lean` now proves `simplex_isCorrelation`, `simplex_sum_row` and `simplex_sum_column` at every n≥2. Source compile and stable target install passed with warnings as errors. The lead independently reviewed all three proofs. Fresh required-endpoint audit `checks/wp21/DefinitionsAudit.lean` passed three endpoints, each exactly `propext`, `Classical.choice`, `Quot.sound`; JSON is `checks/wp21/definitions-audit.jsonl`. The row-sum proof needed sums factored with `← Finset.mul_sum` and identity entries expanded by `Matrix.one_apply`; the initial forward rewrite did not expose the cancellation. No private WSC name was invoked.

The base-case file first proves the actual rectangular Gaussian map of the coordinate sum to `Dirac 0`, then transfers zero sum almost everywhere. At every nonpositive threshold the orthant event is almost everywhere contained in a zero coordinate hyperplane, which has zero measure by the public marginal law. The two-site comparison uses real-valued inclusion-exclusion and actual Gram-law positivity of joint exceedance when G01>−1, including G01=1 duplicates.

## Accepted base-case receipt

`lake env lean -DwarningAsError=true FSC/Simplex/BaseCases.lean` and the lead's stable target installation both exited 0. The lead independently read the full source and accepted the actual zero-sum law, nonpositive comparison, two-site inclusion-exclusion, duplicate-inclusive exceedance positivity and exact equality contract. `two_site_compare` admits every two-dimensional correlation and every real threshold; `two_site_equality` has only the intended positive-threshold hypothesis and concludes equality exactly at `simplex 2`.

Fresh audit command `/home/abhmul/.local/share/agent-python/.venv/bin/python scripts/audit_axioms.py checks/wp21/Audit.lean --required FSC.map_sum_simplex --required FSC.simplex_cdf_nonpos --required FSC.two_site_compare --required FSC.two_site_strict --required FSC.two_site_equality --save-json checks/wp21/audit.jsonl` exited 0 for all 15 endpoints. Every report has exactly `propext`, `Classical.choice`, `Quot.sound`; expanded required statements are preserved.

The actual adverse file compiles warning-clean and its six-endpoint fresh audit also passes, saved in `checks/wp21/adverse-audit.jsonl`. A duplicated two-score covariance satisfies strict comparison at every positive threshold. A separate rank-one three-score covariance with scores `(Z,−Z,Z)` is a correlation different from the simplex, yet has CDF zero at every nonpositive threshold, hence equality with the simplex there. Its law is proved by the actual rectangular Gaussian map. This guards against extending positive-threshold rigidity to zero or negative thresholds.

Failed proof/API experiments are retained in this card: measurable/open set intersection constructors required explicit set-intersection targets before function inference; a simultaneous rewrite of the intersection and complement equations failed where one pattern was absent; `measureReal_univ_eq_one` was rejected under warnings as errors and replaced by current `probReal_univ`; concrete matrix/vector displays needed explicit transparent definitions or `change` for their final finite values. These repairs introduced no new assumptions.

SHA-256: `Simplex/Definitions.lean` = `b8680158f522c9d2b40438bcb7aa4235d1bf1d26975dfc6d8882deab1a8366e2`; `Simplex/BaseCases.lean` = `e4fbc657ddd505917b4b4278826454fe811663b6e8409af5f37f0d9998f2e6e3`. `git diff --check` on this owned scope exited 0. No conditional analytic input remains in these endpoints; higher-dimensional comparison and its final acceptance remain separate unfinished work.
