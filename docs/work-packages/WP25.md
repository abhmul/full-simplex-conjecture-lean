# WP25: Independent statement and clean-checkout acceptance

- Status: integrated; all implementation acceptance gates passed
- Owner: root integration/Git/audit runner; analytic_preflight owns IndependentStatements.lean and statement-review card; wp00_algebra owns provenance.json and provenance-review card; wp00_imports owns its WP24 environment preparation only
- Dependencies: WP24, WP10
- Owned files: `FSC/Audit.lean`, `docs/RELEASE_AUDIT.md`, `checks/release/`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: PUBLIC_CONTRACT.md; pro-return/ENVIRONMENT_AND_ACCEPTANCE.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Independently print/unfold public types, inspect transitive axioms, run tests and artifact-free clean-checkout reproduction. Lead updates release root only after accepted modules.

## Acceptance contract

Closed unconditional theorem, exact pins, warning-clean builds, fresh whitelist audit, source/license ledger and reproducible clean-checkout evidence. Public release remains a separate operator action.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Final Comparison, Equality and Main targets are installed warning-clean. Root owns FSC/Audit.lean, final public-root integration, fresh required-endpoint auditing and artifact-free reconstruction. The independent reviewers own the disjoint files specified above. No clean-checkout success is claimed yet. The process-local no-cache and concurrency controls were checked against pinned Lake source and a compiled four-module overlap probe; see checks/wp24/CLEAN_REPRODUCTION_PLAN.md.

The complete current-checkout runner passed: checks/release/current-gates-v2/verification.json records warning-clean full/root builds, all45 public audit declarations, all17 expanded declarations, 27 regression audit files, 12 parser tests and both deliberately rejected actual-Lean calibration cases. The optimized-Python runner invocation was separately rejected as intended. This is mode current-checkout, not the pending artifact-free-source-reconstruction mode. The first run's exact name-coverage failure remains preserved; only #print qualification changed, not proofs or the parser. Independent statement, provenance and runner reviews are in the three WP25 review cards.

Final acceptance: source candidate 1915b3485c8d61e531374261e7844010b7538eb9 was reconstructed in new independent Git source clones with zero startup compiled artifacts and every pin verified. The actual no-cache full source build passed all3839 jobs with warnings treated as errors; the separate root compile, public45/expanded17/regression216 axiom reports, parser tests and both negative calibrations passed. Three additional already-committed adapter files contributed four more fresh reports in the same clean checkout. Root/source/package status and all pins remained unchanged. The independent reviewer checked all40 command exits/log hashes and the actual raw reports. Evidence is checks/release/clean-source, clean-adapters and clean-preparation.json; RELEASE_AUDIT.md records exact commands, measured durations and the certified revision.

The maintained runner now includes those three adapter files in its fixed coverage map (30 regression files total); a fresh consolidated40-command integration run passed. Source-equivalence.json verifies all135 tracked Lean/pin/audit-parser/test/clone-helper files byte-identical to the certified candidate and an AST comparison proves that the runner's only change adds regression requirements. Later evidence metadata is not represented as the same Git tree as the certified source. No mathematical hypothesis, trust dependency, pin, original source repository, public remote or license was changed.
