# WP20: Abstract compact strict barrier

- Status: integrated; independently reviewed and freshly audited
- Owner: root; owns FSC/Threshold/LinearBarrier.lean and this card before edits
- Dependencies: WP01
- Owned files: `FSC/Threshold/LinearBarrier.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/INTERFACES.md abstract barrier; pro-return/DOSSIER.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove the abstract [0,T] compact shape/threshold barrier from bounds, continuity, D(0)=0 and minimizer slopes.

## Acceptance contract

Ordinary theorem with every abstract premise explicit; no derivative of minimum envelope, tail limit or rigidity dependency.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

2026-09-08: `FSC.compact_slope_comparison` has the exact explicit abstract signature from INTERFACES.md and proves comparison for every nonnegative threshold. `FSC.slope_eq_of_touch` derives equality of fixed-shape slopes from comparison on positive thresholds and a positive contact. Both have no project-specific analytic oracle. The comparison uses a minimum on `univ ×ˢ Icc 0 T`; endpoint exclusions make its threshold interior, and the positive penalty contradicts the supplied minimizer slope.

`lake build FSC.Threshold.LinearBarrier --wfail` exited 0. `checks/WP20Audit.lean` prints both expanded statements and their transitive axioms. Independent source review by worker analytic_preflight checked the positive penalty, both endpoint exclusions, shape minimality, fixed-shape derivative and noncircular equality step. The stronger all-real bounds in the chosen generic signature match the CDF application's available bounds.

Preserved compiler diagnostics: a derivative proof using `simpa` left function/operator instances mismatched; a typed scalar linear derivative plus direct `.add` resolved it without changing the statement. `IsMinOn`'s set-membership representation initially prevented rewriting `f t`; an explicit `change f t - D t ≤ f s - D s` resolved the helper goal. No derivative of the minimum envelope, tail limit or reference-shape assumption was introduced. Remaining consumer WP22 must provide compact correlation space, CDF continuity and the fully discharged minimizer slope theorem.
