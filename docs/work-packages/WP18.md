# WP18: Duplicate and positive-definite exclusions

- Status: reviewed; integrated at the travel-pause milestone
- Owner: root, sole owner of Exclusions.lean and checks/wp18
- Dependencies: WP04, WP09, WP13
- Owned files: `FSC/Minimizers/Exclusions.lean`, `FSC/Gaussian/Replacement.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/SUPPORT_NOISE_VARIATION.md section 5; pro-return/DOSSIER.md exclusions
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Replace a duplicate by independent noise; use L=G-delta J for PD descent, with general row positivity already proved.

## Acceptance contract

A positive-threshold global shape minimizer is distinct and non-PD; no circular use of singular facets.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Root read teaching manuscript 9.2 and reviewed SUPPORT_NOISE_VARIATION section 5, with the latter's normalized PSD-addition route preferred. Frozen outputs: every positive-threshold global correlation minimizer is distinct and non-PD. The duplicate replacement must have an actual Gaussian law and CDF exactly phiCDF(t)*cdf(G,t). PD exclusion uses a proved positive delta with G−delta*J PSD and the now-closed universal normalized-addition derivative, with general kappa positivity established before singular facets. No singular Gram or facet hypothesis may enter these exclusions.

Root split the exact independent-coordinate replacement into Gaussian/Replacement.lean before its first edit. This reusable finite replacement law is separate from the minimizer contradiction. Public CFC spectral positivity supplies a positive scalar identity lower bound for PD matrices; the accepted simplex PSD law gives J≤nI, hence an actual positive delta with G−delta*J PSD. This avoids reproving a sphere minimum or guessing an openness API.

Accepted actual laws: `map_replacementScore` derives the masked-plus-independent score law from rectangular Gaussian maps and the actual product sum law. `replacementCov_isCorrelation` proves feasibility. `cdf_replacementCov_of_duplicate` proves exactly `cdf replacement t = normalCDF t * cdf G t` for every real t under a true duplicate pair; its event removes only the already enforced duplicate inequality. `duplicate_coordinates_ae` is derived from the actual ordinary Gram law. Positive CDF and normalCDF<1 then yield `distinctScores_of_cdf_minimum`.

Accepted PD exclusion: `exists_pos_sub_allOnes_posSemidef` gives actual delta>0 with G−delta*J PSD from the public spectral lower bound and simplex PSD. `not_posDef_of_cdf_minimum` uses the closed normalized-addition derivative and actual global-minimum trace inequality; the exact trace is −delta*kappa and previously proved general row positivity makes it negative. Final `cdf_minimum_distinct_not_posDef` needs only n≥3, correlation, t>0 and the actual global covariance minimum. No padded or intrinsic rank premise, full-stress certificate or analytic oracle enters it.

At base f9d6e92, `lake build FSC.Minimizers.Exclusions --wfail` exited0. Fresh required-endpoint audit on checks/wp18/Audit.lean passed13 declarations; checks/wp18/Adverse.lean passed4. Exact printed types and axiom output are audit.jsonl and adverse-axioms.jsonl in that directory, all closures subsets of propext/Classical.choice/Quot.sound. Adverses prove that replacing one duplicate produces the actual identity covariance and CDF normalCDF(t)^2, and that positive-threshold identity/duplicate configurations are not global minima.

Independent analytic-worker review accepted the whole replacement law/event argument and spectral subtraction, then the final PD/minimum wrapper. The algebra worker independently read the final exclusions and confirmed the trace sign, strict replacement and lack of circular rank assumptions. Compiler repairs were local: Matrix.mulVec_diagonal was initially guessed in reversed word order; explicit diagonal expansion compiled. Lambda is reserved syntax and was replaced by scalar name a. An importing consumer started before Replacement.olean existed, was rejected, then rerun successfully after target installation. The first duplicate adverse encoding did not elaborate definitionally; its rejected compiler JSON with generated error axioms is preserved as adverse-failed-encoding.jsonl. Explicit matrix extensional equality fixed it, and the fresh audit passed. These failures were not acceptance evidence.
