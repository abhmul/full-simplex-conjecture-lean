# WP12: Singular rank-padded Gram and tangent realization

- Status: reviewed; installed and freshly audited with adverse consumers
- Owner: `/root/wp00_imports`, assigned by lead on 2026-09-08 before edits
- Dependencies: WP02
- Owned files: `FSC/LinearAlgebra/PaddedGram.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md representation decisions
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Represent any non-PD correlation in dimension n-1 and identify each unit-normal tangent with dimension n-2.

## Acceptance contract

Law-preserving padded realization and tangent isometry, with no intrinsic-rank restriction on final consumers.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The lead assigned exclusive ownership of `FSC/LinearAlgebra/PaddedGram.lean`, this card and `checks/wp12/` before edits. The existing ordinary Gram realization and public affine Gaussian law remain the foundations. This package pays dimension reduction only for the energy consumer: a singular correlation has a unit-normal realization in dimension `n-1`, and a unit-normal tangent there is realized in dimension `n-2` for `n≥3`. Lower intrinsic rank is retained by padding. Search pinned orthonormal-basis, submodule and finite-dimensional isometry interfaces before implementing representation helpers. No source/Git/pin outside these assigned files is owned by the worker.

## Exact accepted local statements

```lean
FSC.exists_padded_unit_gram_law
    {n : ℕ} (hn : 1 ≤ n) (G : Mat n)
    (hG : WeakSimplex.IsCorrelation G) (hnot : ¬G.PosDef) :
    ∃ v : Fin n → Coord (n - 1), (∀ i, ‖v i‖ = 1) ∧ Matrix.gram ℝ v = G ∧
      (stdGaussian (Coord (n - 1))).map
        (fun y ↦ WeakSimplex.Coord.ofFun fun i ↦ inner ℝ (v i) y) =
        multivariateGaussian (0 : Coord n) G

FSC.paddedTangentCoordinates
    {n : ℕ} (hn : 3 ≤ n) (u : Coord (n - 1)) (hu : ‖u‖ = 1) :
    (ℝ ∙ u)ᗮ ≃ₗᵢ[ℝ] Coord (n - 2)
```

The tangent coordinate map preserves the full ambient Euclidean norm and inner product. `FSC.map_paddedTangentCoordinates_stdGaussian` proves that it transports the actual standard Gaussian on the orthogonal submodule to the standard Gaussian in dimension `n-2`. The chosen realization imposes no exact-rank condition. The ordinary score-law consumer `FSC.map_score_stdGaussian_of_gram` works in every ambient dimension and reuses the public pinned WSC law.

## Source construction

Read `DOSSIER.md` representation decision at line 270 and the actual support-cap dimension argument at lines 106 onward. Exact pinned source consumers are `Matrix.posDef_gram_iff_linearIndependent`/`Matrix.posDef_gram_of_linearIndependent`, `linearIndependent_of_top_le_span_of_card_eq_finrank`, `Submodule.orthogonal_eq_bot_iff`, `Submodule.exists_mem_ne_zero_of_ne_bot`, and `OrthonormalBasis.fromOrthogonalSpanSingleton ... .repr`. If the n ordinary Gram vectors spanned their n-dimensional ambient space, they would be linearly independent, making their Gram matrix positive definite. Thus non-PD gives a proper span and a nonzero vector orthogonal to every normal. Its orthogonal hyperplane has dimension n-1, even when the Gram span has smaller intrinsic rank. Transporting the normals through the orthonormal coordinates preserves their Gram matrix and unit norms. The same public singleton-complement construction identifies the later unit-normal tangent with dimension n-2.

This is a routine representation refinement of the dossier's suggested square-root-kernel route: the ordinary realization still consists of square-root rows, but the exact Gram/linear-independence API finds their containing hyperplane without separate kernel calculations. The implementation lead was informed before the construction. No changing intrinsic rank is introduced, and no private declaration or source adaptation is used.

## Local verification and scope

`lake env lean -DwarningAsError=true FSC/LinearAlgebra/PaddedGram.lean` exited 0. Importing audits are prepared in `checks/wp12/Audit.lean`; actual rank-two triangle, rank-one duplicate-three and n=3 tangent-law consumers are in `checks/wp12/Adverse.lean`. The only core compiler repairs were specifying the ambient span for `Submodule.subset_span` when elaboration could not determine the existential membership type, and using the suggested direct `simp ... at hu` for the impossible zero unit vector to keep warnings clean.

The tangent-law theorem starts from the actual standard Gaussian on the orthogonal submodule. The additional identity between an ambient projected-residual pushforward and its subtype-embedded standard Gaussian is not claimed here yet; this is a separate bridge for the subsequent conditional-cap/energy consumer. The fixed-dimensional padded score law itself has every hypothesis discharged.

## Acceptance receipts

The lead installed `FSC.LinearAlgebra.PaddedGram --wfail`, exit 0, and independently reviewed the complete source. The exact fixed `n-1` realization and Gaussian score law, `n-2` orthogonal isometry and standard Gaussian transport were accepted as stated. The residual-to-subtype bridge remains explicitly separated and is now active WP16 work.

Fresh required-endpoint importing audit of `checks/wp12/Audit.lean` passed nine declarations. `lake env lean -DwarningAsError=true checks/wp12/Adverse.lean` exited 0; its fresh required-endpoint audit passed three actual consumers. Every endpoint and consumer uses `[propext, Classical.choice, Quot.sound]`. JSON evidence is `checks/wp12/axioms.jsonl` and `checks/wp12/adverse-axioms.jsonl`. Audit command form: specialized-environment Python `scripts/audit_axioms.py FILE --required NAME ... --save-json OUTPUT`, requiring every printed declaration in the corresponding source file. The adverse consumers prove the singular rank-two triangle law in Coord2, a genuinely rank-one duplicate-three law still in Coord2, and the n=3 tangent's standard Gaussian law in Coord1.

The adverse compiler first interpreted field notation on an untyped function as `Function.PosDef`; the corrected statement uses explicit `Matrix.PosDef`. An unannotated natural parameter in the concrete tangent law caused an elaboration heartbeat timeout; specifying `(n := 3)` compiled immediately without raising the heartbeat limit. These failed representations did not alter accepted mathematics.
