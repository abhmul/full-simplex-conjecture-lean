# WP11: Unbounded dilation, energy and exact padding

- Status: integrated; independently reviewed and freshly audited
- Owner: root; ownership of Dilation.lean, Padding.lean, this card and checks/wp11 recorded before edits
- Dependencies: WP01, WP02
- Owned files: `FSC/Support/Dilation.lean`, `FSC/Support/Padding.lean`, `FSC/Gaussian/StandardDensity.lean`, this card and checks/wp11
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/DOSSIER.md energy and padding; pro-return/PROBES.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove density scaling and a uniform integrable derivative majorant; strict energy positivity for positive supports in dimension>=1; product padding identity.

## Acceptance contract

Dilation identity without bounded caps; square strip energy includes unused Gaussian directions.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Root started the independent dilation/energy branch after the actual-law and ordinary Gram adapters compiled. Read teaching manuscript 7.1–7.4 and the reviewed energy/padding interfaces. Preserve unbounded caps, a derived uniform majorant, full ambient squared norm and the exact unused-coordinate contribution. Initial source work targets set dilation and Gaussian moment/energy adapters, then the parametric integral consumer; these helpers alone do not accept WP11.

Root additionally owns FSC/Gaussian/StandardDensity.lean, a narrow adaptation of WSC DensityRatio.lean's private density declarations (source lines 16–45 and 64–74, pin a204c53cae45652d12524132dbb9a2e0ffe8cf78). The exact upstream MIT notice is preserved in the adapted file; imported StatLean code keeps its own existing Apache-2.0 provenance. Changes expose public names and specialize the index to Fin n without changing normalization or hypotheses. No generated private declaration name is used, and this does not assign a license to other new FSC work.

## Accepted analysis and laws

FSC.hasDerivAt_capMass_dilate w b proves the ordinary derivative at s=1 of capMass w (s • b) equals d * capMass w b − capEnergy w b. It has no boundedness, unit-normal, support-positivity or rank hypothesis; the index type can be arbitrary. The proof first derives the positive scaling law and exact Lebesgue density integral, then proves the uniform derivative bound on [1/2,3/2]. Its dominating function is a constant times (1+‖y‖²) exp(−‖y‖²/8), whose integrability follows from actual Gaussian second moments and scalar change of variables. Parametric integration is applied on the original possibly unbounded cap. No regularity premise is assumed.

FSC.capEnergy_pos w b requires only a finite index type, ambient dimension d≥1 and every support b_i>0. The strict cap is an open neighborhood of zero; its punctured part has positive actual Gaussian mass. The nonnegative-integral support criterion proves strict energy positivity. FSC.capEnergy_eq_of_hasFDerivAt is deliberately a generic chain-rule corollary with an explicit actual support derivative premise; it equates ambient energy to d times mass minus the frozen canonical support-gradient contraction. WP05 must provide that premise downstream.

FSC.map_splitCoords_stdGaussian proves the actual larger-dimensional Gaussian maps to the product law under EuclideanSpace.finAddEquivProd. FSC.norm_sq_splitCoords explicitly proves the SUM of squared Euclidean norms; the ordinary product norm is a maximum and was never substituted for it. FSC.capMass_padNormals preserves cap mass, and FSC.capEnergy_padNormals w b k proves energy increases by exactly k * capMass w b. The energy identity allows any index type and uses the actual larger ambient space, including d=0 or k=0.

## Verification and adverse consumers

Warning-clean direct compilations and stable Lake installations of StandardDensity, Dilation and Padding exited 0. Fresh specialized-environment scripts/audit_axioms.py checks/wp11/DensityAudit.lean passed 5 endpoints; checks/wp11/Audit.lean with required hasDerivAt_capMass_dilate, capEnergy_pos and capEnergy_padNormals passed 18 endpoints. Expanded types and transitive reports are in density-axioms.jsonl and audit.jsonl. Each uses only propext, Classical.choice and Quot.sound.

The importing checks/wp11/Adverse.lean proves the actual interval mass and truncated second moment using WSC's public truncated_second_moment. It identifies the padded cap with [-t,t] × ℝ, proves energy 2H−2tφ(t) for H=2Φ(t)−1, dilation flux tφ(t), and strictly positive normalized energy H−tφ(t) for t>0. The unused Gaussian direction contributes the second H. The same check proves the actual unbounded redundant halfline's dilation derivative tφ(t) and energy Φ(t)−tφ(t). Fresh required-endpoint auditing passed all 8 declarations; output is adverse-axioms.jsonl.

Independent worker wp00_algebra inspected all three complete source files and the pinned upstream density slice. Review found no blocking issues in normalization, volume Jacobian, domination, strict positivity, actual product law or ambient energy. The reviewer confirmed that the support-gradient corollary is the only explicitly derivative-conditional endpoint.

## Failed API and representation evidence

Matrix.toEuclideanCLM is square-only; the earlier WP02 rectangular adapter is kept separate from padding. Padding uses the public finite-coordinate product-measure equivalences, with an explicit pointwise equality to the Euclidean equivalence. Their function coercions are not definitionally identical because piCongrLeft transports dependent coordinates; simplification of the public equivalence application lemmas resolves this without casts hidden in instances.

The first density integration probes required the ENNReal scope and fully qualified pinned Vendor.StatLean names. In the chain-rule consumer, simplification must establish the base point 1 • b before composing; current HasFDerivAt elaboration does not automatically reduce that product. The square/halfline audit initially rejected an unclosed definitional composition goal and accordingly reported sorryAx for the failed declarations. The failed compiler JSON is preserved as adverse-failed-composition.jsonl; the repaired importing audit above is fresh and contains no forbidden axiom. No failed run is counted as acceptance.

Next consumer: actual support-gradient/energy compatibility in WP16, after singular rank padding and the canonical pair-support normalization. The generic dilation and padding package does not supply those missing geometry hypotheses.
