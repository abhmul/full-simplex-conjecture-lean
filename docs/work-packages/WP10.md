# WP10: Singular analytic integration probe

- Status: integrated; independently reviewed and freshly audited

Integration status update at source candidate 1915b34: the later universal derivative specialization is also complete. FSCProbes.UniversalAnalyticAdverses.triangle_universal_derivative and triangle_trace_coefficient prove the exact match to the independently computed triangle; the final acceptance suite reruns both the independent and universal audits. Earlier pending labels below are historical evidence, not outstanding mathematical inputs.

Final graph reconciliation: the planned BoundaryControls.lean split materialized as FSCProbes/UniversalAnalyticAdverses.lean, containing the universal triangle/five-score and duplicate/antipodal controls. The machine-readable owned-file graph now points to that actual compiled module.
- Owner: worker analytic_preflight, assigned by integration lead 2026-09-08; integration and Git remain with lead
- Dependencies: WP03, WP04, WP05, WP07, WP08
- Owned files: `FSCProbes/SingularTriangle.lean` and this card assigned to analytic_preflight; `FSCProbes/BoundaryControls.lean` remains unassigned
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/PROBES.md; MATHEMATICAL_REVIEW.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

First carry the explicit rank-two triangle independently through real laws, support derivatives, Peano averaging and a rank-increasing path. This concrete certificate precedes acceptance of universal WP09. After WP09 is integrated, add a separate specialization/regression check; that later check is not a prerequisite for the initial WP10 certificate.

## Acceptance contract

Kernel-checked triangle derivative -2*q12/5, not merely trace algebra. Start its concrete subcases before universal WP09 finishes.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

The worker reviewed the frozen WP01 definitions against the mathematical source: column orientation, omitted pin events, q domain, NoCoincident conditions, stress factor and Peano2 factor agree. Source preflight is recorded in WP10-preflight-evidence.md. Initial concrete deliverables are the actual Gaussian rank-zero law, canonical triangle pair success probabilities 1/0/1, and the unbounded redundant-cap mass and derivative. These do not accept the full derivative package.

First compiler call, lake env lean FSCProbes/SingularTriangle.lean, exited 1 after 0.8128 seconds because FSC/Support/Definitions.olean was not yet installed. This is an upstream build-order failure, not a mathematical/API failure. The lead owns installation of the shared target; the worker does not bypass it.

## First kernel-checked analytic slice

Extended worker ownership: FSCProbes/SingularTriangleAudit.lean, FSCProbes/TriangleThreshold.lean, a matching threshold audit, and checks/wp10 are assigned by the lead. The threshold slice starts with an exact local event decomposition: when 4*b₁ - 5*b₂ < 3*b₀, triangle threshold mass equals Φ(b₀)Φ(b₁) - Φ(b₁) plus the actual bivariate (scores 1,2) threshold mass. This is equivalent to splitting the min-integral at its crossing, but exposes only one nonsingular bivariate support derivative. It does not introduce covariance calculus or assume the threshold expansion.

On base revision 449f42623d7442b27bff20df165bb9829196b67c with the owned source additions, lake env lean -DwarningAsError=true FSCProbes/SingularTriangle.lean exited 0. Lake build FSCProbes.SingularTriangle --wfail exited 0; the target reported 3.9 seconds, with 3762 dependency jobs mostly reused. This is a concrete probe build, not a release-root FSC certificate.

Accepted declarations in namespace FSCProbes.SingularTriangle:

- gram_isCorrelation and gram_distinct for the explicit 3-by-3 matrix; map_normals_stdGaussian for the actual rank-two Gaussian score law.
- multivariateGaussian_zero_cov for every mean and dimension; pairLaw_01, pairLaw_02, pairLaw_12 for all real pins, obtained from the explicit zero residual covariances.
- pairSuccess_01, pairSuccess_02, pairSuccess_12 give respectively 1, 0, 1 at every t > 0 under the frozen full-index canonical laws and omitted-pin events.
- q_01, q_02, q_12 identify the same frozen weights with φ(t)², zero, and (5/3)φ(t)φ(t/3); q_01_pos and q_12_pos prove the claimed strictness.
- redundant_capMass gives capMass redundantNormals ![a,b] = normalCDF (min a b) for all real a,b under the actual standard Gaussian measure. No bounded-cap assumption is used.
- hasDerivAt_redundant_support and hasDerivAt_active_support give zero and φ(t) at the actual supports (t,2t).
- redundant_sliceLaw, redundant_sliceMass_active, redundant_sliceMass_inactive and redundant_supportGradient identify the frozen slice-law coefficients exactly.
- redundant_support_C1 gives an open neighborhood U of (t,2t), ContDiffOn ℝ 1 of the actual cap mass on U, and HasFDerivAt with the frozen supportGradient at every point of U. Its only scalar hypothesis is t > 0.

Fresh audit: specialized-environment Python scripts/audit_axioms.py FSCProbes/SingularTriangleAudit.lean --required FSCProbes.SingularTriangle.map_normals_stdGaussian --required FSCProbes.SingularTriangle.pairSuccess_02 --required FSCProbes.SingularTriangle.q_12 --required FSCProbes.SingularTriangle.redundant_support_C1 --save-json checks/wp10/singular-triangle-axioms.jsonl exited 0 for 23 declarations. Every transitive report uses only propext, Classical.choice and Quot.sound. The JSON file also contains printed theorem signatures.

Failed API experiments preserved as actionable evidence: broad simplification rewrote Gram diagonals to norm disjunctions; expanding PiLp.inner_apply before simp fixed it. Concrete Fin 3 index 2 did not reduce in nested vector notation until Matrix.cons_val_two and, for equalities, Fin.ext_iff were supplied. Expanding pairResidual twice inside both matrix products exceeded recursion depth; named explicit residual-matrix identities eliminated that expansion without increasing limits. The names lt_mem_nhds and gt_mem_nhds describe eventual inequalities in the opposite orientation to the initial guess; actual compiler types corrected the calls.

The local Lean LSP was later made available by the lead. After reading the mandated agent-python README, the worker used scripts/lean_lsp.py and obtained the precise recursion diagnostics. Its client emitted an EOFError during close; compiler diagnostics and the successful warning-clean compilation are the acceptance evidence.

Independent lead source review confirmed the initial actual-law, canonical event and redundant-cap mathematics. Expanded q/C1 review remains requested. Next critical consumers are the actual singlePinMass-to-cap bridge using WP02's affine-law adapter, the full threshold-vector second-order expansion, bounded-noise averaging, and the rank-increasing derivative. The concrete derivative -2*q12/5 remains unproved, so WP10 remains in-progress.

## Actual-law bridge and full threshold expansion

The first slice was extended with gram_det, gram_not_posDef, singleLaw_0, singlePinMass_0_eq_capMass, singlePinMass_0 and not_differentiableAt_tied_support. The single-pin law and mass bridge hold for every real t; the redundant support simplification uses t > 0. The tied-support negative control proves that b ↦ capMass redundantNormals ![t,b] is not differentiable at b = t, preventing an unnecessary global tie-smoothness assumption. The refreshed singular-triangle audit passes 29 declarations, including the bridge and tie-control as required endpoints, with only propext, Classical.choice and Quot.sound.

FSCProbes/TriangleThreshold.lean now proves the active-region decomposition directly from the actual Gaussian score law and event union/intersection identities. Orthogonal invariance gives the positive-correlation bivariate cap representation and symmetry. The actual product Gaussian law plus indicator Fubini gives bivariateCDF_slicing; the scalar fundamental theorem of calculus gives the left partial, and the law symmetry gives the right partial. Continuous explicit partial derivatives then yield contDiff_bivariateCDF and contDiffAt_thresholdCDF on the open activeRegion. The region contains every common positive threshold.

Accepted endpoint FSCProbes.TriangleThreshold.peano2_thresholdCDF has the full domain Coord 3: for every t > 0 it proves Peano2 of the actual thresholdCDF gram at t·1, with its genuine first and second Frechet derivatives. No conditional analytic fields, full-rank assumptions, covariance derivatives, or tie exclusions occur in this theorem.

Commands: lake env lean -DwarningAsError=true FSCProbes/TriangleThreshold.lean exited 0. Lake build FSCProbes.TriangleThreshold --wfail exited 0 (3770 jobs, threshold target 2.7 seconds). The fresh specialized-Python audit of FSCProbes/TriangleThresholdAudit.lean, requiring thresholdCDF_activeRegion, bivariateCDF_slicing, contDiffAt_thresholdCDF and peano2_thresholdCDF, exited 0 for 12 declarations; checks/wp10/triangle-threshold-axioms.jsonl preserves exact statements and allowed transitive axiom evidence. The lead independently reviewed and accepted the event, actual-law Fubini, C2 and Peano proof slice.

Additional ownership assigned by the lead: FSCProbes/TriangleVariation.lean and its matching audit/check files. Work continues through the actual normalized rank-one noise law. The scalar pullback along e₂ is appropriate for the concrete rank-one perturbation because the full vector expansion is already independently proved; its coefficients will be identified with the frozen canonical q₁₂. WP10 remains in-progress until the actual covariance-path derivative, strict sign and rank increase compile and audit.

Further compiler evidence: Set.indicator needs classical decidability in the Fubini proof; noncomputable section alone does not provide it. Typed constant-one functions avoid mismatched indicator rewrites. Orthogonal invariance must rewrite only the source measure (conv_lhs), because an unrestricted rewrite also transforms the target measure. The scalar Iic integral FTC required convertibility-aware `using!` at a normed-space instance boundary. All were ordinary representation/API repairs; no mathematical hypothesis was added.

## Closed concrete normalized-addition derivative

On base revision ffb94a2100e826f3a0d4975ed5144b25dd370fd2 with the owned additions, FSCProbes/TriangleVariation.lean closes the requested concrete certificate. The exact accepted endpoint is FSCProbes.TriangleVariation.hasDerivWithinAt_normalizedAdd: for every real t with 0 < t, HasDerivWithinAt (fun s ↦ FSC.cdf (FSC.normalizedAdd gram noiseCov s) t) (-(2/5) * FSC.q gram t 1 2) (Set.Ici 0) 0. The endpoint contains the actual multivariate Gaussian cdf and the single frozen q, with no analytic premise beyond t > 0.

The proof establishes a scalar pullback Peano expansion with explicit first/second coefficients, applies the proved bounded-second-moment averaging theorem under gaussianReal 0 1, and cancels normalization drift to -(2/5)q₁₂. map_real_to_coordOne and map_noiseVector construct the actual scalar rank-one Gaussian law. map_add_scalarNoise uses the proved all-PSD independent-sum law; normalizer_cov and map_normalizedNoise identify exactly the frozen normalizedAdd covariance. normalizedNoise_event and indicator Fubini prove cdf_normalizedAdd_eq_noiseAverage for every t and every s ≥ 0, closing the bridge from the derivative of an integral to the derivative of the requested Gaussian probability.

The remaining accepted controls are normalizedAdd_derivative_neg, normalizedAdd_isCorrelation for every s ≥ 0, normalizedAdd_det = s/(1+s), gram_rank = 2, noiseCov_eq_outer with e₂ = noiseVector 1, noiseCov_rank = 1, normalizedAdd_zero, normalizedAdd_rank = 3 for s > 0, and normalizedAdd_rank_increases. Thus this is a feasible singular-to-full-rank perturbation with the required strictly negative canonical coefficient.

Final commands: lake env lean -DwarningAsError=true FSCProbes/TriangleVariation.lean exited 0. Lake build FSCProbes.TriangleVariation --wfail exited 0 (3773 jobs, target 3.9 seconds). Fresh specialized-Python scripts/audit_axioms.py FSCProbes/TriangleVariationAudit.lean required map_normalizedNoise, cdf_normalizedAdd_eq_noiseAverage, hasDerivWithinAt_normalizedAdd, normalizedAdd_derivative_neg, normalizedAdd_isCorrelation, gram_rank, noiseCov_rank and normalizedAdd_rank_increases; it exited 0 for 25 declarations. checks/wp10/triangle-variation-axioms.jsonl preserves the importing audit and exact printed endpoint signatures. Every transitive closure uses only propext, Classical.choice and Quot.sound. The owned proof files contain no sorry, admit, custom axiom or native_decide. The integration lead independently source-reviewed and accepted the entire law/event/Peano/averaging/cancellation/rank slice before the fresh importing audit.

Two intermediate target builds failed only because a concrete Fin 3 index 2 needed Matrix.cons_val_two in the rank-one submatrix proof and the following Fin 1 tactic sequence triggered the unnecessarySeqFocus linter; both were repaired, then the final warning-clean build and importing audit were rerun. No linter, recursion, trust or mathematical assumption was relaxed.

This closes the early concrete WP10 analytic acceptance contract. It does not prove the universal WP09 derivative, generic WP05 support C1, generic WP07 threshold Hessian, padded square-energy probe, or the public FSC endpoints. The worker next owns the generic normalized-law and explicit-Peano derivative adapters under WP09, while the lead retains integration and the later universal specialization check.

## Actual full threshold expansion accepted

The lead subsequently reviewed the expanded q/C1 slice, actual single-pin-to-cap affine bridge, and tied-support nondifferentiability control. The fresh SingularTriangleAudit now covers 29 declarations. The tie control deliberately rules out imposing differentiability at an equal-support reference.

TriangleThreshold.lean has separately compiled and been installed with warnings as errors. Its actual event decomposition, Gaussian rotation/symmetry, product law and Fubini formula give explicit continuous scalar partial derivatives of the bivariate CDF. The proved product calculus adapter then gives global bivariate C2, local C2 of the actual singular triangle thresholdCDF on activeRegion, and full Coord 3 Peano2 at every common positive threshold. Root independently read the complete source, including the actual law and domain restriction. Fresh TriangleThresholdAudit covers 12 declarations with the permitted three axioms; raw output is checks/wp10/triangle-threshold-axioms.jsonl.

Exclusive ownership of FSCProbes/TriangleVariation.lean was assigned to analytic_preflight for the exact-law normalized-addition consumer. The graph depends on ordinary laws, generic averaging and the extracted generic calculus adapters, rather than completion of the universal threshold package: the concrete geometry discharges its own regularity inputs. The closed derivative/sign/rank acceptance recorded above subsequently discharged the remaining concrete obligations. Root integrated all three stable probe modules and their fresh audits into the permanent probe root.
