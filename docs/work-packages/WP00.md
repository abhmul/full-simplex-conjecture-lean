# WP00: Pinned bootstrap and evidence tooling

- Status: integrated
- Owner: root (integration and audit); disjoint Imports and Algebra workers recorded in IMPLEMENTATION_LOG.md
- Dependencies: none
- Owned files: `lakefile.toml`, `lake-manifest.json`, `lean-toolchain`, `FSC.lean`, `scripts/`, `FSCProbes.lean`, `FSCProbes/Algebra.lean`, `FSCProbes/Imports.lean`, `FSCProbes/Audit.lean`
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/ENVIRONMENT_AND_ACCEPTANCE.md; pro-return/PROBES.md
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Resolve exact WSC/mathlib pins, compile the bootstrap/probes, calibrate fresh audit output and record environment. This is infrastructure, not FSC.

## Acceptance contract

Resolved manifest and actual revisions; probe compile/axiom receipts; no mathematical-completion claim.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

2026-09-08: actual pins match Lean 4.31.0, WSC `a204c53cae45652d12524132dbb9a2e0ffe8cf78`, mathlib `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`. Mathlib cache populated locally; WSC dependency sources compiled without edits. `lake build FSC.Definitions FSCProbes.Imports --wfail` exited 0 (3233 total jobs, mostly cache hits); the algebra target was previously installed successfully. Exact source/API consumer statements and failed experiments are in WP00-imports-evidence.md and WP00-algebra-evidence.md.

Fresh `scripts/audit_axioms.py` compiler runs accepted all seven algebra endpoints and all seven Imports endpoints, each with only `propext`, `Classical.choice`, `Quot.sound`. Reports are `checks/wp00-algebra-axioms.jsonl` and `checks/wp00-import-axioms.jsonl`. Real compiler calibration accepted an axiom-free reflexivity theorem and excluded middle (`checks/wp00-audit-allowed.jsonl`, exit 0), and rejected a deliberately isolated foreign-axiom fixture (`checks/wp00-audit-rejected.jsonl`, expected exit 1). The negative fixture is outside all Lake libraries and is never imported by production. All 12 parser unit tests also pass. The actual Lean JSON schema required no parser repair.

There was initially no exposed LSP tool. Following explicit user installation authorization, root installed `leanclient==0.13.2` into `.lake/lean-tooling/python` using `uv pip install --python /home/abhmul/.local/share/agent-python/.venv/bin/python --target .lake/lean-tooling/python leanclient`. The read-only `scripts/lean_lsp.py` helper now returns actual goals and diagnostics from `lake serve`; `checks/wp00-lsp-goal.json` records a successful goal query. No global configuration, shared Python environment, or mathematical dependency pin changed. Early client API failures (no context manager, dataclass diagnostics) were corrected against installed source.

This accepts WP00 infrastructure only. Neither the algebra identities nor the generic explicit-hypothesis API consumers establish the FSC derivative or public comparison/equality.
