# WP09: Universal normalized PSD-addition derivative

Lead integration review: root independently read NormalizedAddition.lean and Analysis/FeasibleVariation.lean in full and accepted the actual Gaussian product/normalization law, finite-second-moment contraction, normalization velocity and diagonal cancellation. The later WP07 producer now discharges the Peano/Hessian inputs; root independently reviewed that closure and has begun its public-root integration. The earlier conditional milestone is preserved below as history, not a current missing obligation.

- Status: integrated; independently reviewed and freshly audited
- Owner: worker analytic_preflight; integration/Git owner remains the lead
- Dependencies: WP02, WP07, WP08
- Owned files: `FSC/Gaussian/NormalizedAddition.lean`, `FSC/Analysis/FeasibleVariation.lean`, matching probes/audits in checks/wp09, and this card
- Governing architecture: ../ARCHITECTURE.md and ../PUBLIC_CONTRACT.md
- Mathematical source: pro-return/SUPPORT_NOISE_VARIATION.md sections 4-5
- Source controls: external_sources allowed; legacy_material selected

## Deliverable

Prove path feasibility and law identity, then cancel normalization drift to obtain one-half trace(SL).

## Acceptance contract

Universal right derivative for every correlation G and PSD L at distinct scores,n>=3,t>0, including rank increase and antipodes. Unit diagonal of G is essential to this normalization formula.

Names and Lean-shaped interfaces in the Pro dossier are proposed until elaborated. Freeze the actual type before substantial proof work, preserving the exact mathematical hypotheses and conclusion. Record source declarations reused and failed API candidates. Missing upstream claims may be explicit parameters in clearly named conditional assemblies, never axioms or hidden instances.

## Verification and handoff

Compile the owned file, install its Lake target before dependent imports, and run the applicable milestone build. Record exact commands, revision, exit status, printed type and fresh transitive axiom report for the acceptance theorem. Only the standard allowed subset is acceptable. Independent review is required for a load-bearing analytic/interface result; setup/probe evidence must retain its narrower status.

Do not mark this package complete on a list of plausible APIs or an uncompiled proof sketch. If blocked, record the exact goal, minimal reproducer, relevant searched declarations and smallest missing mathematical interface; other independent packages may proceed. No arbitrary time/token budget applies.

## Evidence log

Ownership assigned by the lead before edits on 2026-09-08. The worker read SUPPORT_NOISE_VARIATION sections 4-5 and the corresponding normalization interface. Compiler evidence from the closed WP10 triangle motivates splitting the old single-file package: actual normalized Gaussian laws and probability Fubini identities are independent of the threshold regularity producer, whereas the derivative first consumes an explicit genuine Peano2 hypothesis. The lead approved this split and owns central graph integration. The generic Peano-hypothesis theorem remains outside FSC.lean until WP07 supplies and discharges the exact threshold expansion. The final universal q/stress derivative acceptance contract is unchanged.

## Compiled actual-law and explicit-Peano consumers

On base revision 40898073491b55d952be052a3985ec5b4a45dc25 with owned source additions, FSC/Gaussian/NormalizedAddition.lean proves normalizingDiagonal_cov, normalizedAdd_isCorrelation, normalizedAdd_zero, map_sub_sqrt_smul_multivariateGaussian, map_normalizedAddition, normalizedAddition_event and cdf_normalizedAdd_eq_noiseAverage. Feasibility requires the base correlation, PSD L and s ≥ 0. The exact law and noise-average identity need only PSD G/L, any dimension, every real t and s ≥ 0; zero dimension, singular laws, antipodes and duplicated scores are not excluded. The chosen negative independent noise gives the plus noise sign in the threshold shift and has the same PSD-addition covariance.

FSC/Analysis/FeasibleVariation.lean proves the vector normalization drift derivative, finite Gaussian second moments, centered coordinate moments, and integral_bilinear_multivariateGaussian for every continuous bilinear B. The latter gives the double covariance contraction and does not assume symmetry of B. Peano2.hasDerivWithinAt_cdf_normalizedAdd consumes a genuine explicit Peano2 expansion of the actual thresholdCDF, and concludes the normalized CDF right derivative with l(normalizationVelocity) plus half the Gaussian quadratic expectation. The further explicit-hypothesis theorem Peano2.hasDerivWithinAt_cdf_normalizedAdd_of_hessian consumes B(e_i,e_j) = stress G t i j - if i=j then t*l(e_i) else 0 and proves the canonical half-trace derivative. Both hypotheses are visible theorem arguments, not fields or instances. These generic consumers remain outside FSC.lean. The actual threshold Peano/Hessian producer is still required before accepting the universal FSC derivative.

Commands: lake env lean -DwarningAsError=true FSC/Analysis/FeasibleVariation.lean exited 0. Lake build FSC.Gaussian.NormalizedAddition --wfail exited 0 (3086 jobs, target 2.1 seconds), and lake build FSC.Analysis.FeasibleVariation --wfail exited 0 (3095 jobs, target 1.9 seconds). Fresh specialized-Python audit of FSCProbes/FeasibleVariationAudit.lean required the feasibility, map law, Fubini identity, drift, bilinear expectation, and both explicit-Peano derivative endpoints; it exited 0 for 17 declarations. checks/wp09/feasible-variation-axioms.jsonl preserves exact printed signatures and transitive axiom evidence using only propext, Classical.choice and Quot.sound. Independent lead review is requested; this card does not mark the universal package complete.

Compiler/evidence repairs: the public diagonal-vector theorem is Matrix.mulVec_diagonal, not diagonal_mulVec. EuclideanSpace.proj requires explicit scalar/index parameters when composing its MemLp map, and its public coercion theorem is coe_proj. Expanding a general bilinear form initially reversed the nested summation order; Finset.sum_comm repaired the actual expression, preserving the theorem without a symmetry assumption. Deprecated continuous-linear-map sum/smul aliases were replaced by sum_apply and smul_apply. The first audit invocation failed to save because checks/wp09 did not exist; after creating the owned output directory the importing audit was rerun from scratch and passed. No mathematical or trust hypothesis was relaxed.

## Universal analytic inputs discharged

WP07 now supplies the actual threshold Peano and canonical Hessian and concludes FSC.hasDerivWithinAt_normalizedAdd in FSC/Gaussian/ThresholdExpansion.lean. Its only mathematical hypotheses are correlation G, DistinctScores G, PSD L and t > 0; the derivative is exactly half of trace(stress G t * L) on Ici 0 at zero. This supersedes the earlier conditional-only milestone above. The generic Peano theorems remain reusable genuine-hypothesis results, while the universal producer discharges every input through actual Gaussian laws and twice-applied support differentiation. The lead independently read and accepted the final closure.

The fresh required-endpoint importing audit in checks/wp07-threshold/threshold-expansion-axioms.jsonl includes this closed derivative and passed all 28 declarations using only the standard allowed axioms. UniversalAnalyticAdverses separately instantiates it at the singular triangle and rank-two five-site configuration; its fresh 11-declaration receipt is checks/wp07-threshold/universal-adverses-axioms.jsonl. Triangle normalization cancels to precisely the earlier independent -(2/5)*q_12 coefficient. Genuine duplicate/zero-threshold nondifferentiability controls remain explicit. WP07 records the exact build commands, current base revision and compiler evidence. Public-root integration and the full FSC acceptance gates remain the lead's separate responsibility.
